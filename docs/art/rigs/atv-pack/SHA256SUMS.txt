c47d4b8c80cbfeda2e8c924c7899b60f34f01a1684d8355742fdcc05e2117277  atvIsoRig.js
e22d5bb6843dc7c95a45ab2a4a665047a4dcc649b57fdc6ef9dc276dd5715801  _atvKit.js
190d19178fb0378553612e2f654066c64c7fbfbe9ed98274a7fec94bd2bef68c  atvPack.contract.json
4371cfc9941c38e0fb3cae1785f5501d5bdcf37925d3bee6185333698553803b  harness.html
f568439709441d8c5fef66eef0362a4d606497893daadb909bc14a3081f79d50  README.md
ae12b6ca7a9efcdfbd2be3d28dd5de3ae3c561e74f709b4acde908d56d677976  ../gameplay/vehicles/atvIsoRig.atvPack.gameplay.json

# REV 1.1 RE-STAMP (2026-09-11). atvIsoRig.js is now the rev 1.1 file — the merged rev 1.0 plus one
# appended IIFE adding AtvIso.saddleFor(). Three numbers moved in this file and nowhere else:
# the rig (8578eb45… -> c47d4b8c…), and the contract and sidecar that carry the rig hash inside them.
# _atvKit.js, harness.html and README.md are byte-unchanged and re-verified above.
#
# The contract and the sidecar were NOT hand-patched. The sidecar was regenerated by
# AtvIso.gameplayAll() off the rev 1.1 rig and diffed against the merged file: ONE field moved,
# derivedFromRigSha256. gameplayAll() output is byte-identical between rev 1.0 and rev 1.1, and
# render() is identical over 56 probes (3 bodies x 8 facings, plus the amphib's 4 poses x 8), so
# every measured number in the contract — painted_bbox, cue_motion, anchors — still holds.

# Hashes are over the LF-NORMALISED bytes: the form git stores in the blob, and the one number
# that is the same on every platform. `core.autocrlf` is true here and no .gitattributes rule
# covers *.js or this kit's *.json, so a WORKING TREE copy is CRLF after a fresh checkout and
# hashes differently. Normalise before comparing:
#
#   python -c "import hashlib,sys;b=open(sys.argv[1],'rb').read().replace(b'\r\n',b'\n');print(hashlib.sha256(b).hexdigest())" docs/art/rigs/atv-pack/atvIsoRig.js
#
# atvIsoRig.js's hash above is ALSO atvPack.contract.json's `rigSha256` and the sidecar's
# `derivedFromRigSha256`. All three were verified equal on the delivered bytes and again after
# the copy into the repo (2026-09-06, ATV pack intake). Compare the FULL 64-char digest, never a
# prefix: the hightop van's bad stamp shared its rig's first SIXTEEN hex digits and diverged over
# the remaining 48, and a prefix check is what let it through.
#
# If the rig moves and those two stamps do not, the sidecar is STALE: do not read its geometry,
# and do NOT re-stamp it here. docs/art/rigs/** is the art director's lane; a hash corrected on
# our side comes back wrong on the next regeneration and fakes freshness. Register the refusal in
# VehicleRigFleet.SidecarHashRefused and send the re-stamp upstream.
#
# The 22 sheets under reference/ are LFS binaries and are not listed here; git's own oids cover
# them. They are REFERENCE ONLY: these three bodies bake as MESH (ADR 0035, the road fleet's
# path), and no sprite path consumes them.
