# Authoring Scenes — paint your own environments

> **For the owner (no coding needed).** This guide shows you how to *draw* a place in Hidden
> Harbours: paint ground (sand, grass, rock, dirt, wharf, water, shoreline) with a tile brush, and
> drag in decor (trees, buildings, props) like stamps. Everything you need was built by the
> scene-painting toolkit — you just open Unity and paint.

Pixel-art note: our art is **32 pixels = 1 metre**. Tiles are 1 metre cells; a tree sprite is ~2 m
tall. You don't have to think about this — the tools are already set to the right scale.

---

## 0. One-time setup (build the toolkit) — do this once

The tiles, the palette, and the decor prefabs are generated by a menu command. If they already
exist you can skip this; running it again is harmless (it just rebuilds them).

1. In the Unity menu bar, click **Hidden Harbours ▸ Art ▸ Build Scene-Painting Toolkit (tiles +
   palette + decor)**.
2. Wait a few seconds. The Console (Window ▸ General ▸ Console) will say the toolkit is ready.

That single command runs three steps (you can also run them one at a time from the same menu):

| Menu command | What it makes |
|---|---|
| **Build Terrain Tiles** | The paintable ground tiles + an auto-shaping shoreline, under `Assets/_Project/Art/Tilesets/Tiles/`. |
| **Build Tile Palette** | A palette called **HiddenHarboursTerrain** you pick tiles from, under `Assets/_Project/Art/Tilesets/Palettes/`. |
| **Build Decor Prefabs** | Drag-in trees, buildings and props, under `Assets/_Project/Prefabs/Decor/`. |

---

## 1. Where to paint (use a scratch scene for now)

> **Heads-up — which scenes get saved into the project is still being decided.** *Which* scenes
> become hand-authored, committed regions, and *how* your painted scenes get saved back into the
> shared project, is a separate call the lead architect is working out. **Until that's decided,
> practice in a throwaway "scratch" scene** so your painting can't disturb the real game scenes.

To make a scratch scene:

1. **File ▸ New Scene**. Pick the **Basic 2D** (or Empty) template, click **Create**.
2. **File ▸ Save As…** and save it somewhere out of the way — e.g. name it `ScratchPaint` and save
   it under `Assets/_Project/Scenes/` (it's fine while you practise; don't worry about committing it).

---

## 2. Add a surface to paint on

A tile brush needs a **Tilemap** to paint onto. We made a one-click helper:

1. **Hidden Harbours ▸ Art ▸ Add Paintable Tilemap**.
2. Look in the **Hierarchy** (top-left list): you now have a **Grid** containing a
   **TerrainTilemap**. That's your canvas. (Run the command again any time you want a second layer —
   e.g. a separate one for paths or decoration; each new layer is named uniquely.)

The ground layer is set to sit *behind* sprites, so anything you drag in later (trees, buildings)
appears on top of the painted ground.

---

## 3. Open the Tile Palette and paint terrain

1. Open the palette window: **Window ▸ 2D ▸ Tile Palette**. A panel appears (dock it next to the
   Scene view if you like).
2. At the **top-left of that panel** there's a dropdown of palettes — choose **HiddenHarboursTerrain**.
   You'll see the tiles laid out in a row: Sand, Rock, Grass, Dirt, WharfDeck, Foam, Water, and the
   **Shoreline** (auto-shaping) tile.
3. Just under the palette name is a row of **tool buttons**. The ones you'll use most:
   - **Brush** (paintbrush icon) — click a tile in the palette to load it, then click/drag in the
     **Scene view** to paint it onto the tilemap.
   - **Eraser** (hold **Shift** while painting, or pick the eraser tool) — removes tiles.
   - **Box Fill** (rectangle icon) — drag a rectangle to fill an area fast.
   - **Picker** (eyedropper) — click a painted tile to re-load that tile as your brush.
4. **Make sure the right tilemap is the target.** Near the top of the Tile Palette there's an
   **"Active Tilemap"** dropdown — it should show **TerrainTilemap**. If you added more than one
   layer, switch this to the layer you want to paint.
5. Paint! Pick **Sand** or **Grass**, drag across the Scene view, and the ground fills in.

### The Shoreline tile (auto-shaping)
The **Shoreline** tile is special: it automatically picks the right edge/corner art depending on
which neighbouring cells you've painted, so a coastline shapes itself as you draw. Paint it where
land meets water. If a piece looks like it's facing the wrong way, just paint over it again or use a
neighbouring cell — the rule re-evaluates as you go.

---

## 4. Drag in decor (trees, buildings, props)

Decor is placed as **prefabs** — pre-made objects you drag in like stamps. No tilemap needed.

1. In the **Project** window (bottom panel), open `Assets/_Project/Prefabs/Decor/`. You'll find
   three folders: **Trees/**, **Buildings/**, **Props/**.
2. Click a folder and you'll see thumbnails (Tree01…Tree37, Cottage, ShipwrightShed, Barrel, Crate,
   WharfPost, etc.).
3. **Drag a prefab straight from the Project window into the Scene view** and drop it where you want
   it. It appears at the correct size and the right "footing":
   - **Trees** plant at the trunk base — drop a tree where you want its trunk to stand.
   - **Buildings and props** centre on the drop point.
4. To move something after dropping it: click it in the Scene view and drag, or use the **Move tool**
   (press **W**). To rotate/scale, use **E** / **R** — though for pixel art it's usually best to
   leave scale at 1.
5. Place as many as you like. Duplicate a placed one quickly with **Ctrl+D**.

---

## 5. Sorting — making things overlap correctly

In our ¾ top-down view, an object lower on the screen should usually draw *in front of* one higher
up (so a tree in front of a house overlaps it). The prefabs come with sensible default layering
(ground behind, props/buildings/trees above it, the player on top), so most scenes look right with
no fiddling.

If two decor pieces overlap wrongly:

1. Click the piece in the Hierarchy or Scene view.
2. In the **Inspector** (right panel), find the **Sprite Renderer** component.
3. Change **Order in Layer** — a *higher* number draws in front. Nudge the front object up by 1 (or
   the back object down by 1) until it looks right.

That's the only number you'll ever need to touch, and only when something overlaps oddly.

---

## 6. SAVE (don't lose your work)

- **Save the scene:** **File ▸ Save** (or **Ctrl+S**). Do this often.
- Painting a tilemap and dropping prefabs changes the **scene**, so saving the scene saves your
  whole painting.
- The tiles, palette, and decor prefabs themselves are already saved as project assets — you don't
  need to re-save those.

> Reminder: until the lead architect settles which scenes are committed and how painted scenes get
> merged back, keep practising in your scratch scene. When the workflow for "real" regions is ready,
> this guide will be updated with the save-back steps.

---

## Quick reference (menus & windows)

| I want to… | Do this |
|---|---|
| Build/refresh the whole toolkit | **Hidden Harbours ▸ Art ▸ Build Scene-Painting Toolkit (tiles + palette + decor)** |
| Get a surface to paint on | **Hidden Harbours ▸ Art ▸ Add Paintable Tilemap** |
| Pick & paint terrain tiles | **Window ▸ 2D ▸ Tile Palette** → choose **HiddenHarboursTerrain** |
| Place decor | Drag a prefab from `Assets/_Project/Prefabs/Decor/{Trees,Buildings,Props}/` into the Scene view |
| Fix wrong overlap | Select the object → Inspector → **Sprite Renderer ▸ Order in Layer** |
| Save | **File ▸ Save** (Ctrl+S) |

### Where the toolkit lives (for the curious / for developers)
- Tile + palette + decor builders: `Assets/_Project/Art/Editor/` (`TileAssetBuilder`,
  `TilePaletteBuilder`, `DecorPrefabBuilder`, `PaintableTilemapMenu`, `SceneAuthoringMenu`) — all
  Editor-only tooling owned by the tools-editor lane.
- Generated tiles: `Assets/_Project/Art/Tilesets/Tiles/` · palette:
  `Assets/_Project/Art/Tilesets/Palettes/HiddenHarboursTerrain.prefab` · decor prefabs:
  `Assets/_Project/Prefabs/Decor/`.
