# Water fidelity register — the judged list, for the owner to RANK (2026-09-01)

> **Status: DRAFT, awaiting the owner's ranking.** PR 0 of the water-fidelity charter
> (`HANDOFF-2026-09-01-water-fidelity.md`): *"improve water fidelity. I want to continue work on crashing
> washes using the new water physics and improve the overall look and feel entirely."* This is the ADR 0027
> judge pass that has been owed since August, done as an **instrument** rather than an opinion: every row
> below names a plate you can look at, the mechanism that draws it (verified in source, file and line), the
> lane's read of its severity, and what fixes it. **You rank it; PRs 4+ of the charter follow your order.**
> PRs 1–3 (the crashing washes) are already sequenced and are rows 1–4 here.
>
> The instrument is `Assets/Tests/EditMode/WaterFidelityPlateSweepTests.cs`; its output lives under
> `artifacts/water-plates/` (gitignored, regenerated in ~50 s on a GPU). Re-run it before and after every
> later PR of the charter: the before/after pair of sheets **is** the evidence a look PR ships with.

---

## 1. The instrument — what a plate is, and what it is not

**Four world-locked viewpoints**, each photographed across **{glass, light, blow, gale} × {spring low,
mean, spring high} × {noon, golden hour, night with the searchlight}** — 36 plates per viewpoint, a contact
sheet per viewpoint (`SHEET-<viewpoint>.png`, weather across, tide within each weather, hour down), and a
`MANIFEST.txt` beside the plates recording every uniform each plate was pinned to, **read back** from the
globals and the property block after the push, never assumed.

| viewpoint | where the camera looks | why |
|---|---|---|
| `nmc-steep` | Nine Mile Creek, the steepest point of the break contour — ξ = 2.19, **plunging**, weight 1.0 — at (76, −14) | the one stretch that earns a lip and a barrel |
| `nmc-sand` | Nine Mile Creek, the point on the break contour from which the surf runs furthest shoreward (23 m), biased so the beach is in frame — (120.7, −242.3) | the widest spilling beach |
| `stp-arrival` | St Peters, the reef door and apron on the arrival line — (206.5, 0) | the coast every new game sees first; the apron dries at spring low |
| `ww-open` | West Water, the region centre — 6 m of water in every direction, no seabed in frame | the open-water **control**: whatever it shows is the sea, not the shore |

**Every plate is shot through the shipped components.** A fake environment holds one weather and one tide
still; the shipped `WaterSurface` then pushes `_Chop`, `_Roughness`, `_Flow`, `_WindDir`, `_WaterLevel`,
`_RainIntensity`, the weather-mood blend across the preset anchors and the palette seam, exactly as in Play;
the shipped bridge calls publish the wave field, the fetch and the breaker contour from the same
`GameConfig.asset`; the shipped day/night maths publishes the hour; the searchlight is published through
the shipped light bridge. Nothing is written to `Water.mat`.

**The pinned world** (the manifest carries the numbers per plate):
- weather = the sim's own pairing: each sea state's wind is `WeatherModel.WindStrengthFor(seaState)`, so a plate never pairs a gale's foam with a zephyr's wind. glass 0.00 / 0 m·s⁻¹ · light 0.25 / 1.63 · **blow 0.55 / 5.70** · gale 0.95 / 12.95. ⚠ #680 shot its "working day" at 8 m/s and 0.55 together and measured a 0.92 m break depth; the sim's pairing at 0.55 is 5.7 m/s and breaks in **0.60 m**. Both are legitimate; the plates use the sim's. ✅ **RE-VERIFIED 2026-09-10 (water PR B) and the table above stands unchanged.** It was suspected stale after PR D (#811) widened the wind law, so it was re-derived from source rather than re-typed: `WindFor(w) = WindHeading * WeatherModel.WindStrengthFor(SeaStateOf[w])`, and `WindStrengthFor` is a pure lerp over `SeaBandEdges = { 0, 0.5, 2, 4, 6, 8, 11, 14 }` (`WeatherModel.cs` ~134, ~184–193) — **a table #811 never touched**, because #811 moved the wind PROFILE that generates a sea state, not the band edges that name one. lerp(0.5, 2, 0.75) = **1.625**, lerp(4, 6, 0.85) = **5.70**, lerp(11, 14, 0.65) = **12.95**. ⚠️ What #811 DID move is the register's other wind claim: see §3 item 9.
- tide = the region's own spring low / mean / spring high (−2.20 / 0 / +2.20 m).
- hour = noon 12:00; **golden hour 17:00** — found on the shipped profile as the warmest afternoon tint that is still bright, tint (0.866, 0.529, 0.356); **night 02:00**, tint (0.016, 0.020, 0.040), a **new moon below the horizon** published explicitly (`MoonCycle` is Play-only, and the shader's fallback for an UNSET moon is a full moon at the camera anchor — the first draft photographed one).
- the searchlight = `BoatSpotlight`'s serialized defaults, which are what the presentation service mints for the cape: 2.5 m up, warm (1, 0.88, 0.62), intensity 1.5 × water strength 0.8, **9 m range, 26° half-cone**, night gate 0.12/0.35; placed 6 m left of the aim, throwing +x.
- tidal current 0; visibility 1; the object-reflection target bound clear (no reflector exists in a fixture).
- 40 m across at 960 px = **24 px/m**, one plate pixel per cell of the shader's own world pixelize grid.

**What the plates are NOT.** They are the flat `Universal2D` pass: `DisplacedWaterSurface` registers and
ticks only in Play, so the **vertex lift of the displaced sea is absent**, and so is every hull (no
waterline, no wake, no spray, no interior mask). The fragment — every colour, foam, surf, caustic,
reflection and light term — is the same program the displaced pass runs on lifted geometry. A still also
cannot show a beat, a snap or a drift. §3 lists what is owed on those.

**The laws the instrument is built under**, each of which has cost a PR before: a per-pixel diff of two shots
of the same sea measures `_Time`, so every assertion is on the MECHANISM (wet fraction from the terrain,
the published globals, the file's orientation) and judgement is left to the eye; a property block is sticky,
so every scrubbed property is written on every shot; a cold shader cache fakes a regression, so the sweep
warms until nothing is compiling; the night is captured in HDR and the day/night multiply applied on
readback, or it photographs as cream; and **the published file is asserted, never the buffer** — the sheet is
re-loaded from disk and its sea must lie on the side the bathymetry says.

**The knob-by-knob diagnostic** (`TheGlassStripesAndTheGaleShards_MeasuredKnobByKnob`, output in
`artifacts/water-plates/diagnostic/DIAGNOSTIC.txt`) shoots the open-water control at glass and at gale, once
as shipped and once per layer with that ONE property zeroed after the shipped push, and reports the mean
luma, the horizontal-band contrast (std of the per-row mean), the vertical-streak contrast, and the share of
near-black pixels beside a lit neighbour. Whatever a plate shows, the row whose zeroing removes it is the
layer that draws it. Rows 5, 6 and 7 below were named this way, not guessed.

---

## 2. The register — ranked by the lane, to be re-ranked by the owner

Severity is the lane's read of how much of the owner's stated bar each row costs, at the sea the player
sails in (the blow). **The owner's ranking replaces this column.**

| # | in the owner's words | what the plates show | the mechanism, verified in source | severity | ruling owed? | fix |
|---|---|---|---|---|---|---|
| **1** | *"crashing washes"* — **the surf never crashes; it boils in place.** | `nmc-sand-blow-low-noon`: one flat white band hugging the break contour, uniform along its whole length, soft on both sides. No crest arrives, nothing runs up, nothing drains between crests — there is no "one". `nmc-steep-blow-low-noon`: the plunging stretch (ξ 2.19) draws a thin bright line; the lip, barrel and pocket bands sit inside 1–2 m of the break line and read as one line. | `HiddenHarboursWater.shader` ~4385–4470: `surfBreaking` is a depth gate; `ageM` a 16-tap geometric march; `surfAlive = exp(−age/√(g·d)/τ)`; the sheet an `EvolvingField` boil drifting at `_Flow·t·0.5`; the lip a band centred `throwM` past the break line. **No `_WavePhases`/train phase enters the surf block.** | the charter's core | no — already sequenced | **PR 1** #696 (the bore: travel-time march, `Bore01` from the published phase, run-up) + **PR 2** (this PR — the look: `_SurfBeatStrength` beats the sheet from the front and ages it behind, the lip/barrel/pocket travel with the front; `_SurfRunUpStrength` carries the wet edge up the beach on a travel-time whitewater; all dials ship at 0 = today; ADR 0040 rev 3 PR 2 section) |
| **2** | *"churns through different shades of blue, distorts and fades into the ambient ocean"* — **two foam languages.** | `nmc-sand-blow-low-noon`: the surf is flat white and stays white to its outer edge; the wake's blue walk cannot reach it. | Compose at ~4568–4586: `col.rgb = lerp(col.rgb, _SurfColor.rgb, SurfBandValue(cover)·strength)` — a posterized flat white. The surf never enters the foam buffer (`FoamInjectionRegistry`: only hulls inject, through `FoamInjector`), so `WakeFoamAgeing`'s walk through the palette's blues never touches it; the shore fringe is a third churn (`_FoamColor`). | ✅ **DELIVERED** (was high) | no | **PR 8** (`feat/water-look-foam`, Tier A, owner ranking 2026-09-04). The ONE line of the defect: `_SurfColor` is (1,1,1) on **all nine** materials and no preset ever moved it, while `_FoamColor` — the fringe's, the caps', the wake's white — is the preset's own, (0.86,0.84,0.74) in StirredBrown. The walk was lifted out of `WakeFoamAgedColor` into a shared `FoamAgedColor(age01, legacy, strength)`; the wake, the caps and now the surf compose through it, and `WakeFoamKnots`/`WakeFoamRamp3` are reachable from nowhere else. The surf's age is `1 − surfAlive`, its whitewater energy read BEFORE the density lift, the threshold and the posterize `surfCover` goes through. `_SurfAgeStrength`, `_CapAgeStrength` and `_WakeFoamAgeStrength` all at **1** on all nine (one language = one walk at one strength; each 0 is still the exact per-layer passthrough). Measured on the production maths: the drawn band goes from **1** colour to **53**, born at `_PaletteFoam` exactly and dying at `_PaletteMid` exactly — the whole 1.208 of the ramp. Sheet: `SHEET-foam.png`, both arms shot in ONE call with a stated 0-px noise floor. ⚠️ **`_SurfDepositStrength` deliberately stays 0**: the deposit fires on the BORE's pulse, and the drawn surf has no front until `_SurfBeatStrength` comes up — it goes up with the beat, in the same nod (§3 item 6). The fringe keeps its flat white (no age: the wet edge is renewed continuously) and the barrel keeps `_SurfBarrelColor` (a hollow is a shadow, not foam) |
| **3** | (tuning) — **the surf dials are on no material.** | — (the plates draw the shader's `Properties` defaults for all 17 `_Surf*` keys) | `grep -c '_Surf[A-Z]'` = **0** on `Water.mat` and on all eight presets. The keys are not in `MoodFloatNames`, so once serialized they are live — today the owner cannot reach the surf at all. ⚠ `Apply water preset` is a wholesale copy: a preset missing a key stamps 0. | medium (blocks tuning) | no | **PR 2** (this PR): all 17 + the 4 new (`_SurfBeatStrength`, `_SurfRunUpStrength`, `_SurfFrontSlope`, `_SurfDepositStrength`) + the 3 colours serialized on `Water.mat` and the eight presets at today's values; `EveryWaterMaterial_CarriesEverySurfDial_Serialized` keeps it so |
| **4** | *"waves crashing in and out"* — **the swash runs on its own clock.** | The wet edge in `nmc-sand-blow-low-noon` / `stp-arrival-blow-low-noon`. A still cannot show the beat; it shows the edge (and its comb, row 9). | `BeachSwash` (~2639): `base = t·_SwashSpeed·2π + depth·_SwashWavelength`; shipped `_SwashSpeed 0.16` = one run-up every 6.25 s, `_SwashAmplitude 1` (contour metres, slope-true), `_SwashEdgeShift 0.6`, cap `_SwashMaxEdgeShift 0.35`, `_SwashCalmGate 0.7`. Reads `_Time` and `depth` only — neither the trains' phase nor the surf's `ageM`. A bore and the swash do not know each other. | high once row 1 lands | no | **PR 2** (this PR): with `_SurfRunUpStrength` up the drawn edge is `lerp(swash, runUp, breaking)` — it rides the bore where one is alive and drains between crests, and keeps its cosmetic beat where nothing breaks; measured +2342 px of drawn water up the sand shoal's beach, 0 removed |
| **5** | *"glass calm is sacred"* — **the mirror is a striped rug.** | `ww-open-glass-mean-noon`, `nmc-sand-glass-mean-noon`, every glass and light column: hard horizontal wavy bands every ~1.6 m across the whole sea, vertical glint streaks through them; at 17:00 the same stripes in orange. | **Named by the diagnostic:** `_ReflectionStrength = 0` takes the row-band contrast from **0.165 to 0.001** and the sea's mean luma from **0.46 to 0.05** — the stripes are the sky reflection and nothing else, and without it the calm sea is nearly black. `SkyReflection` (~2995–3010): `smearLen = _ReflectionSmear(1.6)·lerp(4,1,sharp)`; `band = 0.5+0.5·sin(2π·(pp.y+(surf−0.5)·smearLen)/smearLen)`; `band = pow(band, lerp(0.4, 3, sharp))` — at calm `sharp = 1`, so the "mirror" is a **1.6 m sine stripe, cubed**, in the sky colour (`_DayNightTint` at `_ReflectionSkyTint 0.85`, strength 0.92 from the calm anchor). The sun streak and the clouds each add ~0.1 luma but no band. | high — this is what the sacred state looks like | **yes** — the mirror's form is a look call | theme: **swell legibility / the mirror's form** (Tier A; `col.rgb` only) |
| **6** | *"waves must be physical"* — **in a blow the water body is black, and everything you see is foam.** | `ww-open-blow-mean-noon`, `nmc-sand-blow-mean-noon`: a camouflage of soft grey blobs and diagonal lanes on black; no crest, no trough, no face is legible as a wave. Mean luma of wet pixels at noon **0.06–0.07** in a blow against **0.45–0.67** at glass. | **Diagnostic (gale):** `_Roughness = 0` → luma **0.002** (the sea without its foam is black); `_FoamConvergenceStrength(0.4) = 0` → **0.016** (the convergence lanes are three quarters of the light); `_StormFoamLaneStrength(0.75) = 0` → 0.049; `_SwellReadStrength(0.35) = 0` → 0.098 (the read **darkens** the sea). The body: `_PaletteDeep` (0.07, 0.10, 0.13) under the storm anchor, `_DeepBlueStrength`, and `ReflectionStrength()` gone by `_ReflectionFadeChop 0.6`, so nothing lights it; `_SwellFaceShade 0.22` is not legible at 24 px/m. | high (P1: the owner's "rigid pattern" verdict, "hard to tell" the big wave is coming) | **yes** — and the `_OceanSwellScale 0.07` ride≠drawn question sits here: **ask, never choose** | theme: **swell legibility and crest form under the relief light** (Tier A for shading; Tier B if anything hulls ride moves)<br>⭐ **THE DARKNESS IS NOW AUDITED IN NUMBERS (water PR B, 2026-09-10), and the owner is the REFLECTION — not the body.** `ReflectionStrength()` is three compounding multiplies, `master × chopFalloff × windDim`, and `WaterReflectionLadderTests` (new, headless, no editor slot) walks all three across the four sweep seas at the shipped four anchors and the **pushed** `_Chop`/`_Roughness`: glass `0.876 = 0.876 × 1.000 × 1.000`; light airs `0.493 = 0.840 × 0.624 × 0.942`; **a blow `0.0095 = 0.623 × 0.0197 × 0.771`**; a gale `0.000 = 0.209 × 0.000 × 0.415`. As a fraction of the glass reflection: 1.000 → 0.563 → **0.011** → 0.000, against row 25's measured luma fractions 1.000 → 0.532 → 0.036 → 0.064. **The mirror tracks the measured cliff almost exactly from glass to light airs and is already gone by the blow.** Subtracting it at k = 0.3208 luma per unit strength (from the row-5 reflection-off diagnostic, 0.05) leaves a residual body of **0.050 / 0.018 / 0.009 / 0.021**. The body is not dark *as well*: at a blow the body is ALL there is, and it is 0.009.<br>⚠️ **The term that does it is mood-INVARIANT.** `_ReflectionFadeChop` is **0.6 on all nine materials** and is not in `MoodFloatNames`, so the chop falloff does not walk with the weather the way the master and `_ReflectionWindFade` do — and it alone goes 0.624 → **0.0197** between light airs and a blow, a 32× collapse, while the master falls only 0.840 → 0.623 and windDim only 0.942 → 0.771. One knob, held still on purpose, is most of the cliff.<br>⚠️ **A second, independent defect, confirmed in this row's own diagnostic: `_SwellReadStrength` can only SUBTRACT.** Zeroing it at a gale takes the sea 0.06–0.07 → **0.098** — the read makes the water DARKER than no read at all. A shading term with no lit side cannot make a face legible on a sea already judged too dark, whatever the onset is set to; it is re-measured at a blow by the knob arm and belongs in the same ruling.<br>⚠️ **This row and row 25 do not agree on the number, and this PR does not pretend to settle it.** Row 6 says mean wet luma at noon **0.06–0.07** in a blow against **0.45–0.67** at glass; row 25 says **0.012** against **0.331**. Row 25's pair is a sweep MANIFEST reading at one named cell (`ww-open`, mean tide, noon), so the ladder and the option table below are priced on it; row 6's pair is not traceable to a single manifest row. **The blow arm's own baseline row arbitrates**, and it is the one measurement in this PR that still needs an editor slot. |
| **7** | *"whitecaps… foggy white soup"* — **in a gale the whitecaps are dark shards.** | `ww-open-gale-mean-noon`, `nmc-steep-blow-low-noon`: hard-edged angular **black** shapes, a repeated mirrored silhouette, cut into the foam lanes. | **Named by the diagnostic:** `_WhitecapTexStrength(0.865) = 0` and the shards vanish (the lanes become speckle); `_ObjectReflectStrength = 0` changes nothing (they are not the reflection target). The whitecap block: `capField = lerp(capField, capPat, _WhitecapTexStrength)` — the painted `_WhitecapTex` stamp sheet (16 mirrored copies of the owner's one mark, `_WhitecapTexScale 0.0625`) REPLACES the cap field; the mark's silhouette is hard-edged, and where the sheet is empty the field falls to 0 — a hole in the lane. Same slot measured 2026-08-05 at **1.35 % coverage in a blow where the procedural field gives 9.95 %**. Painted slots PLACE, not decorate. | high in a gale, medium in a blow | **yes** — the mark and the slot's strength are the owner's art | theme: **foam-language unification** (Tier A) — or the owner repaints the sheet |
| **8** | *"as long as the end look gets pixelated"* — **the shallows wear a patchwork of 4 m rounded squares.** | `nmc-sand-blow-low-noon` and `-golden`, right of the surf band: rounded-square patches ~4 m across with wavy lines inside and hard borders, offset by half cells; present in every low-water plate over sand. ⭐ **MEASURED 2026-09-07, offline, no editor slot** (`WaterUntileMixTests` + the real PNGs through the shipped arithmetic). ⚠️ First, a correction to this row's own note: the caustics do **not** run at 4 m — the call site passes `_PaintScale * 2.0`, a **2 m** cell. The **4 m** slots are `_SurfaceTex` and `_FoamTex`, both at plain `_PaintScale 0.25`. **The mechanism is the LAST LINE of `UntileSampleW`**: it returns `lerp(raw, untiled, _UntileStrength)`, and the raw tap IS the tile grid, perfectly periodic — keeping `1 - s` of it keeps that proportion of the grid drawn however good the untiler is. Measured repeat autocorrelation at the cell lag, five instants each: **`_SurfaceTex` 4 m — 0.000 → 1.000 · 0.500 → 0.711 · 0.644 (shipped) → 0.471 · 0.750 → 0.317 · 1.000 → 0.193**; `_CausticTex` 2 m behaves the same (0.438 shipped → 0.154 at 1.0). **And mid-range is the worst place to stand**: `raw` and `untiled` are decorrelated, so the mix's variance is a parabola with its floor INSIDE the interval, and the shipped 0.644 sits at that floor — field contrast 0.1305 → **0.0770** → 0.0979 across 0 / 0.644 / 1.0. Two hypotheses died and are recorded so they are not re-run: the partition-of-unity variance collapse at cell centres is real arithmetic but small (0.642 vs 0.689), and *"translation cannot remove a period"* is simply false (1.0 gets to 0.193). A per-cell MIRROR variant was measured and buys almost nothing (0.193 → 0.118), so it is not proposed. | ✅ **DELIVERED** (was medium-high) | ✅ **RULED 2026-09-08** — *"give the go ahead to continue on water fidelity i approve their decision"* | ✅ **SHIPPED: `_UntileStrength` 0.644 → 1.0 on all nine water materials.** Strictly better on BOTH axes — **59 % less repeat and 27 % more contrast** — and free, because a slot already pays all five fetches at any strength above zero. Likely stale tuning: 0.644 reads like a value dialed back to hide the two-variant seam that #443 removed, i.e. a number that stopped being true when what it measured was edited. `EveryWaterMaterial_CarriesTheRuledUntileStrength` pins it on the assets, because `Apply water preset` is a wholesale copy (row 3) and a preset that quietly kept 0.644 would stamp it back the next time the owner changed the sea's mood. ⚠️ **A NOTE, NOT A GATE**: the offline half cannot say whether the owner's *"~4 m"* patches are `_SurfaceTex`/`_FoamTex` at 4 m or the caustics' two 2 m lattices beating — both are present over sand at low water and **both are cured by the same knob**. The owner judges the shallows in play |
| **9** | (the shoreline) — **the wet edge is a comb.** | `nmc-sand-blow-low-noon` (left), `stp-arrival-blow-low-noon`: the dry/wet boundary and the drop-off wear 1–3 m "teeth" — hair-like streaks aligned with one axis. | ⭐ **NAMED 2026-09-07, no editor slot spent** (`SeabedBakeCombTests`). The wet edge is `clip(_WaterLevel - lerp(_HeightMin, _HeightMax, tex2D(_HeightTex, uv).r))` — every term a number, so the drawn edge's deviation from the sim's true contour is arithmetic, with no camera, no palette and **no `_Time`** (what killed the rendered sweep). Walked along the contour on Nine Mile Creek's own constants: **spring high 2.1 cm RMS · mean 4.9 cm · spring LOW 28.7 cm**, and at spring low the same bake with EXACT values reads **1.2 cm**. **The 8-bit `R8` height texture is 96 % of it.** The law: every quantum here is a quantum of ELEVATION, the player sees a distance along the GROUND, and the exchange rate is the seabed slope — the beach falls 7 m in 24 m (0.32), the shelf spring low bares falls 1.6 m in 60 m (**0.035**), so half a code is 7 cm of edge at mean water and **67 cm at low**. ⚠ The charter's *mechanism* was wrong while its suspect was right: there are **no texel runs/plateaus** — the map is `FilterMode.Bilinear`, which interpolates BETWEEN codes, so the field is continuous; 8 bits jogs each texel NODE by half a code. `_ShoreNoise` is **exonerated for the clipped edge** (it perturbs a cosmetic `depthC`, never `clip()`); it may still wiggle the alpha fringe. | ✅ **DELIVERED** (was medium) | no — it was a defect, not a look call | ✅ **SHIPPED: the height bake is `R16`.** `SeabedBakeMath.FormatPreference` = R16 → R8, the last rung always answering (the same shape as `FoamBuffer.FormatPreference`, which widened one row earlier because eight bits could not hold a DECAY); `WaterSurface.HeightFormat()` probes once per graphics device; the write is `SetPixelData<ushort>` (**not** `SetPixels32`, which would round-trip the codes through 8 bits and hand back the very quantization the format removes) and `TryReadBakedElevation` reads `<ushort>` or `<byte>` **by format**. Measured after: **spring high 0.7 cm · mean 3.8 cm · spring low 1.2 cm**, landing ON the exact-valued bake at every tide. Rule 7: 256² goes **64 KB → 128 KB** per sea; the rejected alternative was more texels (1520 at R8 still leaves 13.5 cm at 24× the memory). ⚠ What remains at mean tide is **3.8 cm of SPATIAL error from the 2.97 m pitch — row 31's ground, deliberately untouched here** |
| **10** | — **the deep/shallow boundary is a wall.** | `nmc-sand-blow-low-noon`: pale shallows meet black deep water at a hard, hairy edge ~1 m wide. | ⭐ **NAMED 2026-09-07, same fixture, and it is the SAME LAW as row 9.** The shipped material does run the block (`_Turbidity 0.25`, `_UseSeabedTex 1`, `_AbsorptionBands 6`), and a posterized ramp has no soft edge — **every band boundary is a hard step by construction**, so the question is not what makes it hard but WHERE THE SIX STEPS LAND. Measured through `WaterAbsorption`, the shader's own C# twin: the band DEPTHS never move (0.58 / 1.08 / 1.75 / 2.77 / 4.97 m — fixed by sigma); what moves is how much GROUND they occupy. **At spring high the waterline is on the 0.44 beach and the first four stack at 14.0 / 15.2 / 17.0 / 20.4 m offshore — 1.2, 1.7 and 3.4 m apart, four hard steps inside six metres — then nothing for 67 m.** At spring low, on the 0.035 shelf, the same four spread over twelve metres. One vertical quantum divided by one slope, reading as a comb at the edge (row 9) and a wall behind it (row 10). | medium | look — **the band COUNT is the owner's**, the mechanism is settled | the knob is `_AbsorptionBands` and its 0 removes the symptom by construction. ⭐ **THE TABLE (rule 6, 2026-09-08, §43.1)** — measured at spring high, where the bands crowd: **0 = continuous** (the ramp fades, no terracing) · **4 → 2 steps in the first 30 m, tightest pair 2.7 m** · **6 (shipped) → 4 steps, 1.2 m** · **8 → 6 steps, 0.8 m** · **12 → 9 steps, 0.5 m** · **16 → 12 steps, 0.3 m** (about seven pixels — continuous in all but name). Fewer bands = wider terraces, each a harder edge; more = a smoother fall and less of the posterised read. **Six is an art choice, not a bug** — the owner picks |
| **11** | *"the light needs to affect the environment"* — **golden hour tints the frame; the sea has no sun side.** | `nmc-sand-blow-low-golden`, `ww-open-glass-mean-golden`: everything multiplied orange — surf band, mirror stripes, beach alike; no warm/cool split, no face turned to the low sun. | ADR 0013's whole-frame multiply (tint (0.866, 0.529, 0.356) at 17:00) over a fragment whose only sun terms were `_SwellFaceShade 0.22` and the glitter. | ✅ **DELIVERED** (was medium) | no | **PR 9** (`feat/water-look-sun-side`, Tier A, `col.rgb` only). `_SunSideStrength` (new, 0..2, **1.7** on all nine) adds a **COLOUR** where `_SwellFaceShade` adds a **VALUE**, on the SAME signed facing — the facing was hoisted so there is one field slope turned into one normal, shared. The two colours are not authored: they are the published `_DayNightTint`'s deviation from its own grey (+0.282, −0.055, −0.228 at 17:00 — orange, derived) and that deviation NEGATED for the lee face, which is the complement the sky throws into a shadow. No new palette constant for a preset to forget, and a colourless light self-cancels with no divide and no guard. **Gated by `sin⁴` of the solar arc angle** — `_SunElevation` is `cos(solarX·π/2)`, so `(1−e²)²` is 0 at solar noon and 1 at the horizon, times a `smoothstep` that puts a sun BELOW the horizon out of the term (`sin⁴` alone reads 0.151 at 02:00). ⚠ The trap it was built around: **the golden hour's sun is NOT low** — sunrise 6 / sunset 20 puts solar noon at 13:00, so 17:00 is `e = 0.6235` and a naive `1−e` gate would be weakest exactly where this row complains. The gate reads **0.374 at 17:00 against 0.0025 at the plates' 12:00, a 152× separation.** Measured on the production maths (`SunSideMeasurementTests`, no GPU): the warm/cool split over the swell's OWN faces, as a **centroid over faces**, is **0.06298 in r−b = 1.00× the mirror's post-PR-5 row-band contrast (0.063)** while adding only **0.14×** of it in luminance — a HUE signal, not a second brightness layer competing with the mirror. Both controls: a term that lifts BOTH faces scores a split of 0.0001 (0.16 % of it) while genuinely brightening, and the signed GREY `_SwellFaceShade` already in the sea gets an **incidental** 0.00513 for free from the tint multiply (grey × orange is orange) — **the sun side is 12.3× that accident**, which is the number that answers "none is legible". Sheet: `SHEET-sunside.png`, four cells, 0-px noise floor on every one: golden hour moves 1.05 % of the shoal's water and 8.70 % of the open sea, **noon moves exactly 0 px and a glass calm exactly 0 px** (it shares the modelled swell's calm gate, so row 5's sacred mirror is untouched). ~25 scalar ops, no new texture read. ⚠️ **What it cannot show is row 25 below** |
| **12** | *"navigable later by lights"* — **at night the surf vanishes and the searchlight lights nothing you can name.** | `nmc-sand-blow-low-night`, `ww-open-blow-mean-night`: black; the surf that was white at noon is invisible; the shipped lamp is a faint wedge. | The profile's 02:00 tint is (0.016, 0.020, 0.040). The water's light term **multiplies the sea's own colour** by the cone (`_BoatLightBrighten 2.5`), and a black sea × 2.5 is black; `DN_COMP_MIN_CHANNEL 0.02` floors the compensation. ⚠ **#691's eyeball plates were shot at tint (0.075, 0.092, 0.150) with a 2.6 / 30 m lamp** — a 4× brighter night, 1.7× the intensity, 3.3× the range of the lamp the cape actually carries (`BoatSpotlight` defaults 1.5 × 0.8, 9 m). | medium-high for P5 nights | **yes** — the night level, and the shipped lamp against the plates he nodded at | theme: **light on water**; the boat-lights PR 2 gate |
| **13** | — **the mirror does not fade with the light; it fades with the wind.** | `ww-open-light-mean-noon`: at sea state 0.25 the stripes of row 5 still dominate at three quarters of their glass contrast. | `ReflectionStrength()`: `1 − smoothstep(0, _ReflectionFadeChop 0.6, _Chop)` × wind dim — at chop 0.25 the mirror is still about 0.6 strong (0.62 × a 0.93 wind dim). | low-medium | look | with row 5 |
| **14** | — **a bright patch at the channel mouth.** | `stp-arrival-blow-low-noon`, far right: a white blob where the dredged approach meets the reef at low water. | Observed, not yet named — the fringe and the surf both thicken where the depth contours crowd against the flat-bottomed cut (ADR 0040 ruling 2 says a steep wall should THIN the surf, so this wants the knob method before it is called a defect). | low | — | theme: **the shore and shallows** |

### Rows carried from the charter that the plates cannot show

| # | item | why the plates cannot show it | who owes what |
|---|---|---|---|
| 15 | ~~**ride ≠ drawn at `_OceanSwellScale 0.07`**~~ — the vertex stage draws the swell ×2.8 shorter than the field the rock was tuned on | the flat pass has no vertex lift | ✅ **CLOSED — bookkeeping, 2026-09-06.** Ruled by the owner 2026-09-02 (*"swell scale you can make the changes for a longer slower swell, thats fine"*) and **acted on in water-fidelity PR 6**: `_OceanSwellScale` 0.07 → **0.025** on all nine materials, which is the constant the shader normalises by, so the visual frequency scale is **exactly 1** and the drawn sea IS the field. Re-measured and pinned in **#759** (`WaveSpeedMeasurementTests.TheDrawnWaveIsTheModelledWave_AtTheShippedSwellScale`), which also found that the pre-PR-6 error ran the OPPOSITE way to the one assumed — the ratio is `1/√fs`, so fs 2.8 drew waves at **0.60×** the speed their drawn length allowed, too SLOW rather than superluminal. The row was left open in this table after PR 6 closed it; this entry is the bookkeeping. |
| 16 | the wake round-2 eyeball (#669) | no hull in a fixture | owner's eye in Play |
| 17 | `SprayEmitter` sorting over decks (order 5 against every `BoatVisualDef`'s 1) | no hull | a Play-mode look in rough water |
| 18 | the interior-mask "dry island" when a crest stands in front of a hull | no hull, no displaced pass | owner verdict pending since 2026-07-25 |
| 19 | rain streaks and rain rings in the editor | no rain condition in the matrix (visibility pinned 1) | a second sweep with a fog/rain column, if ranked |
| 20 | the interval snaps the 8 Hz push tier may still leave | a still is a still | a frame-pair capture in Play |
| 21 | the fetch feel verdict (`WaveFetch.Strength`) | felt, not seen | owner, at the helm |
| 22 | **the moon's DISC and GLITTER PATH are not cloud-aware** — a full moon behind full overcast still lays a glitter path on the water, on a night whose tint lift the cloud correctly took away | it IS shown now: `SHEET-night-corners.png`, FULL MOON · OVERCAST (added by PR 4) | mechanism named: `MoonCycle.ComputeState` packs `_MoonPhaseState` brightness + presence from the CLOCK alone (phase, arc height) and reads no weather; the water's moon content is gated by those two numbers, so nothing downstream can know it is cloudy. Fix is one shared cloud factor — the same one `MoonlightLift` now uses — applied where the moon is published. Deliberately NOT folded into PR 4 (one theme per PR) |
| 23 | **there is no CLOUD-COVER axis in the sim** — "cloudy" is inferred from fog visibility ⊔ sea-state gloom | every overcast plate in this register is a LOW-VISIBILITY plate wearing the word "cloud" | `EnvironmentSample` carries `Visibility` and `SeaState01` and nothing else; `DayNightMath.WeatherDim` takes the larger. Adequate for M1 and stated rather than invented (PR 4 was explicitly told not to add one). An M2 weather item: a real cloud axis would separate "thick fog on a still clear night" from "overcast", which today are the same input |
| **24** | **the whitecaps JUMP to the end of the foam ramp instead of walking it** — so at `_CapAgeStrength` 1 the milky residual in a GALE all but disappears | it IS shown now, on `SHEET-foam.png`'s gale row: 381 867 px of water move and their mean falls from 0.031 to 0.002 (frozen arm, where the caps are nearly the only content) | mechanism named: `WhitecapLifecycleWave` returns `breakCore` and `residual`, and #719 derived `capAge01 = residual / (breakCore + residual)`. Across the crest that is graded; behind it `breakCore` is 0 and the age is pinned at 1 over the whole back face, so a cap composes at `_PaletteMid` ((0.18, 0.24, 0.28) in `Water_StormGrey`) almost immediately. The WAKE takes its half-life to get there; the cap arrives instantly. Deliberately NOT fixed in PR 8: a graded cap age is a new mechanism and the ranking asked for a dial-and-blend. One dial (`_CapAgeStrength`) reverses it without touching the surf — **the owner's call on the gale row of the sheet** |
| **25** | **the sea is drawn near-black at every sea state where the modelled swell is awake** — so a swell-shading term can only ever land on water the player cannot see | found while shipping row 11, on the sweep's own manifests. Open water at MEAN tide, mean wet luma: glass **0.331** → light **0.176** → blow **0.012** → gale 0.021 at NOON (golden hour 0.148 / 0.087 / 0.015 / 0.020). It is not a West Water quirk and not a golden-hour one: `nmc-sand` at a blow is **0.454 at spring low but 0.026 at mean tide and 0.019 at high** — the bright low-water plate is exposed SAND, not water — and `stp-arrival-blow-low-golden` is 0.059. A **15× cliff between light airs and a blow, at noon**. | Two shipped decisions meet here and neither is wrong alone. (a) The modelled swell's calm gate is `smoothstep(_SwellReadSeaStateLo 0.28, _SwellReadSeaStateHi 0.45, _Chop)` — the owner's 2026-07-08 tuning, so that a glassy calm shows no read — which means `_SwellReadStrength`, `_SwellFaceShade` and now `_SunSideStrength` are all **0 at glass and at light airs** and only awake at blow and above. (b) At blow and above the drawn sea is near-black. The intersection of "the swell reads" and "the water is visible" is therefore very nearly EMPTY. Which layer owns the darkness is not yet named — the row-5 diagnostic showed the calm sea is 0.05 with the reflection off, and the reflection fades with tilt, so the mirror may simply be most of the daylight on open water. **The knob method has not been run on a BLOW sea; that is the first step.** | high — it gates the judging of row 11, it is most of what "the overall look and feel" means on open water, and ⭐ **it now has a PLAYTEST behind it (2026-09-08, register row 35 folded in here)**: the owner called the midday sea *"layers"* at sea state 0.181 where the gate reads **0.000**, and *"really good"* at 0.341 where it reads **0.294**. ⭐ **And the glass fear does not bind**: the wind law is `3 + strN·1.3 + gustN·1.2`, so wind never falls below 0.50 m/s and **the sea state can never fall below 0.143 — a glass calm of 0.05 is UNREACHABLE**. At onset **0.15** the gate at that calmest possible sea is still exactly **0.000**, so row 5's mirror is untouched; 0.10 puts 0.041 on it. Full onset table in §45 | ✅ **RULED 2026-09-09 — "go with 0.10"**. He was offered 0.15 (the lowest onset that leaves the calmest reachable sea at exactly 0.000) and took **0.10**, buying a legible midday face (gate 0.136 at his 13:12 instead of 0.000) at the stated cost of a faint **0.041** on the calmest water the game can draw — a real, ruled change to row 5's mirror. `_SwellReadSeaStateHi` stays 0.45. The second direction (**fix the darkness itself**) is NOT closed by this: the 15× luma cliff between light airs and a blow is still there, and row 25 stays OPEN on that half. | ✅ **SHIPPED (water PR A, 2026-09-09)**: `_SwellReadSeaStateLo` 0.28 → **0.10** on the shader default and serialized explicitly on all nine water materials (as #783 did for row 8's `_UntileStrength` — "Apply water preset" is a wholesale `CopyPropertiesFromMaterial`, so an unstated preset is how the old onset comes back). Guard: `WaterSwellReadOnsetTests` — the onset table through the shader's own arithmetic, the nine materials read BOTH as bytes and as what `AssetDatabase` deserializes, and the shared-axis consequence stated in numbers (see below). ⚠️ **The pair has a SECOND reader**: `SwashSeaStateGate()` keys the same two thresholds for the shore's calm fade, so the ruling also lets the swash wash a little harder on a calm sea (swash gate at the calmest reachable sea 0.300 → 0.329 at the shipped `_SwashCalmGate` 0.7). If the owner wants the old shore back the knob is `_SwashCalmGate`, not the onset. ✅ **RULED 2026-09-09: "accept both"** — the shore's livelier calm fade ships with the onset rather than being decoupled behind its own thresholds. So this is a ruling, not a finding: the swash change was put to the owner with its numbers before it merged, and he took it. ⚠️ It follows that `_SwashSeaStateLo/Hi` do NOT exist and should not be added on the argument that the two readers "ought" to be separate — one axis, one place to tune, ruled. ⚠️ **No plate**: the 15:51 evening picture is the acceptance; the midday one is owed a plate when an editor slot opens. Earlier note, kept: NOT PR 9 — deliberately; PR 9 refused to widen the owner's calm gate as a workaround for a defect that is not row 11's <br>⚠️⚠️ **SUPERSEDED IN PART BY PR D (2026-09-09).** The floor quoted here (wind never below 0.50 m/s, sea state never below 0.143) was true of the wind law as it stood, and it was the argument this row's ruling was priced against. **PR D widened that law**, so the floor is now **0.000 m/s / sea state 0.000** — a glass calm is reachable at last. ⭐ **The ruling survives and is better for it:** at the new floor the swell-read gate is **0.0000**, and at a true Glass sea (0.25 m/s, sea state 0.071) it is **0.0000** as well, so the 0.041 this row priced as the cost to row 5's mirror is **no longer paid at all** — the mirror is now both reachable and unshaded. The cap itself never bound: see row 36.<br>⭐⭐ **THE OTHER HALF IS NOW PRICED (water PR B, 2026-09-10). The owner of the darkness is named, and NOTHING HAS BEEN MOVED — the table is the deliverable and the choice is the owner's.** The audit is in row 6: the cliff is `ReflectionStrength() = master × chopFalloff × windDim`, and `_ReflectionFadeChop` (0.6, identical on all nine materials, absent from `MoodFloatNames`) takes the falloff 0.624 → **0.0197** between light airs and a blow. With the mirror subtracted at k = 0.3208 the residual body is 0.050 / 0.018 / **0.009** / 0.021 — at a blow the body is all there is.<br>**Three options, priced at three recoveries of the blow.** Every cell is the predicted mean wet luma at `ww-open`, mean tide, noon; the blow column is the target and the other three are what it costs. Published by `WaterReflectionLadderTests` (headless, EditMode, no GPU) so the arithmetic is re-runnable:<br>`target 0.044 (a quarter of light airs)`<br>`  (a) _ReflectionFadeChop 0.80        glass 0.331  light 0.212  BLOW 0.044  gale 0.021`<br>`  (b) new _SkyScatterFloor 0.032      glass 0.331  light 0.189  BLOW 0.044  gale 0.053`<br>`  (c1) shared body x 4.6              glass 0.509  light 0.239  BLOW 0.044  gale 0.096`<br>`  (c2) storm anchor + 0.092           glass 0.331  light 0.181  BLOW 0.044  gale 0.105`<br>`target 0.088 (half way back to light airs)`<br>`  (a) _ReflectionFadeChop 1.12        glass 0.331  light 0.239  BLOW 0.088  gale 0.023`<br>`  (b) new _SkyScatterFloor 0.077      glass 0.331  light 0.208  BLOW 0.088  gale 0.098`<br>`  (c1) shared body x 9.5              glass 0.755  light 0.326  BLOW 0.088  gale 0.199`<br>`  (c2) storm anchor + 0.218           glass 0.331  light 0.187  BLOW 0.088  gale 0.222`<br>`target 0.176 (a blow as bright as light airs)`<br>`  (a) UNREACHABLE by (a) alone        glass 0.331  light 0.271  BLOW 0.163  gale 0.049`<br>`  (b) new _SkyScatterFloor 0.167      glass 0.331  light 0.245  BLOW 0.176  gale 0.188`<br>`  (c1) shared body x 19.3             glass 1.245  light 0.500  BLOW 0.176  gale 0.405`<br>`  (c2) storm anchor + 0.471           glass 0.331  light 0.200  BLOW 0.176  gale 0.454`<br>**How to read it.** (a), (b) and (c2) all cost the GLASS column *exactly* nothing, and not by tuning: (a) because at a dead calm `_Chop` is 0 and `smoothstep(0, fadeChop, 0)` is 0 for every positive fadeChop — row 5's mirror cannot be reached by this knob at ANY value, and that is asserted at fadeChop ∈ {0.6, 1, 2, 8, 64}; (b) because its rise is keyed to the fall it compensates; (c2) because the storm anchor's blend weight at a calm is 0.0000. **(c1) is the only one that cannot be made free to the calm sea** — and it is the only one that needs no new uniform and no widened `Range`. The GALE column is the mood price: (a) is nearly free there too (0.021 → 0.023 at half recovery) but undoes "a storm doesn't mirror" in kind; (c2) lands hardest, 2.6× whatever it gives the blow.<br>⚠️ **What (a) also costs, and must NOT be bundled with:** `_ReflectionFadeChop` is declared `Range(0,1)` in the shader, so anything above 1.0 needs the Range widened before a material can hold it (the HLSL has no upper clamp). And `ReflectionSharpness()` is a **separate** curve (`_ReflectionChopScatter` / `_ReflectionWindScatter`) that should probably NOT move with it: a storm that mirrors SHARPLY is a different picture from a storm carrying a broad smear of sky.<br>⚠️ **Tide and hour.** `ReflectionStrength()` and `ReflectionSharpness()` take `(chop, roughness, fadeChop, windFade, master)` and nothing else — no tide, no hour — so every multiplier above is tide- and hour-INVARIANT; what changes with tide and hour is the luma it is applied to. At `ww-open` there is 6 m of water in every direction, so tide moves the wet luma not at all; on `nmc-sand` the bright spring-low blow plate (0.454) is exposed SAND, as this row already records, so the prices apply to the wet fraction only. At GOLDEN HOUR the measured luma is 0.148 / 0.087 / 0.015 / 0.020, and the two options that are pure additions carry straight across at the half-recovery setting: (b) at 0.077 gives `0.148 / 0.119 / 0.091 / 0.097`, (c2) at 0.218 gives `0.148 / 0.098 / 0.091 / 0.220`. (a) and (c1) need a golden-hour reflection-OFF control to fix their own k, which the blow arm shoots when a slot opens. ⚠️ Every predicted column is a LINEAR extrapolation from ONE measured point per sea: arithmetic for ranking the options, not a plate. **The plate is the proof.**<br>⚠️ **The frame these numbers were taken through, named so they cannot expire silently.** The sweep's capture path is a bare `Camera` into an ARGBHalf target — no `UniversalAdditionalCameraData`, no `Volume`, no colour grading; the fixture does not reference the word. The only operation after the water draws is ADR 0013's day/night MULTIPLY, replayed in C# from `DayNightMath.DayNightTint(...)` because that screen-space pass does not run in a fixture. **So the MoodGrade Volume the player sees through is NOT in these plates, and every luma above is the water's own output, pre-grade.** #828 (the grade tone-down) moves `MoodGradeMath` / `MoodGradeDirector` / `MoodGradeProfile` and touches no DayNight file, so neither thing in this frame is in its diff and these numbers survive it landing. What they are NOT is the final frame: rule on the RATIOS between the seas, not on an absolute the player never sees unfiltered. |
| **26** | *"foam fades behind boat but doesnt disperse in width, the foam seem to come from the cetnre of a boat when turning and not accuratly from the stern"* (2026-09-04) and *"the foam always stays to the original foam path, it doesnt widen over time and fade away"* (2026-09-06) — **the foam TRAIL is rooted amidships, and cannot widen** | in play, the cape under way and turning at St Peters | ⚠️ **Not `BoatWakeEmitter`, which the charter named** — the deposited V arms are correctly stern-anchored and guarded at every heading (`WakeDispersalTests.TheDepositRoot_IsAtLeastHalfAHullAstern_AtEveryHeading`, whose failure message already quotes his 08-07 wording). Both halves live in `FoamInjector`, which lays the persistent BUFFER mark: (a) it read `transform.position` and is added to the hull HOST, so the capsule was laid centre→centre and on a turn the centre traces a tighter arc than the transom; (b) `RadiusMeters` is a CONSTANT set once from the half-beam, and the advect pass deliberately never blurs (its cell law makes every scroll an integer texel Load so a wake cannot smudge) — so fading in place was the only thing a laid trail could do. | **(a) ✅ DELIVERED · (b) ✅ DELIVERED** | no | **(a) PR 11a** (`feat/wake-at-the-stern`): laid at the transom via `FoamBuffer.SternWorld` (pure/static), foreshortened by the hull's own bake elevation — on the cape the anchor spans **4.15–6.45 m** of screen through a turn where an unprojected one is a constant 6.45, and that 2.3 m difference IS the breathing gap the plume anchor was fixed for once already. The offset is DATA (`HullMeshDef.WakeSternOffsetMeters`) authored at LOA/2 on **all 34 hull defs** — which is also the root error the shipped trail carried, per hull: dory 2.25 m, console skiff 3.50 m, **cape 6.45 m** (over half her length), tanker 55 m. ⛔ **The WIDTH is untouched by 11a and still stays the beam.** <br>**(b) PR 11b — the area-preserving stamp.** Two measurements shaped it and both are in `FoamTrailRoot*Tests`' history: a compose-only reveal **cannot** widen, because the drawn band already sits at **78 % of the stamp radius** (1.86 m of 2.4 m on the cape) where the falloff is 0.425 and decaying to 0 — it moved the band 1.86 → 1.91 m, and you cannot reveal width that was never stamped; and a naive stamped skirt widens but **multiplies** the foam (drawn integral 9356 → 13052), because coverage ACCUMULATES over passes so "faint enough to hide while young" is not one constant but a function of speed, deposit rate and dt. The term: normalise the injected profile so widening the footprint scales the amount by the ratio of the profiles' integrals — a skirt then changes the DISTRIBUTION of the foam laid, never its total, and conservation holds at INJECTION rather than being patched at compose. Guard: the per-age-bin statement (integral at age t ≤ integral at birth × the decay term), never a whole-trail total — the dispersing arm legitimately draws old foam the shipped arm had dropped below threshold. Sabotage arm: the naive skirt must FAIL it. ⚠️ The age axis is `1 − freshness` (the axis the COLOUR walk already rides), not seconds recovered from it: freshness is 8-bit on a 4 s half-life, so at 20–24 s one code is worth 0.72–1.45 s while coverage still has 6–10 % on it. <br>**(b) SHIPPED as PR 11b** — `docs/design/water-rendering.md` §35 carries the derivation, and two corrections to the charter's own statement of the term came out of it. **(i) The conserved quantity is the disc's AREA integral, not its lateral cross-section**: a parcel is stamped for its whole DWELL under a passing hull, so what lands on a metre of track is `rate · 0.575π · r₀² / speed`, and conserving `1.5·r₀` instead lays **1.4×** the shipped foam. **(ii) An ADDITIVE buffer retains the family's ENVELOPE, not its current member** — nothing takes foam back out of the core as a profile spreads — so conservation is stated on `max over w of falloff(d/w)/w` and the stamp lays that envelope ONCE at each lateral distance, as its edge sweeps outward. Both dials' passthrough is exact (spread 0 leaves the injector's amount line untouched, bit for bit); the dispersing share β is DERIVED from the envelope (`1 − (4/3)·F(r₀/w_max)`, = 7/15 at the shipped 2.5 half-beams) and is not a third dial. MEASURED, cape at 8 kn, all arms in one run: foam laid per metre of track **6.3205 shipped → 6.3416 dispersing (1.003×)** against the sabotage skirt's **8.72×**, and the walked shipped arm reproduces `ShippedFoamPerMetre` to 0.00 %. The band's drawn half-width goes from **2.06 m at EVERY age** to 2.69 → 5.06 m over the first 12 m astern (**2.61×**), and the conservation is speed-free (0.983× at 3 kn, 1.005× at 14 kn). The dispersed foam carries the age it was laid at, or it would be born at `age01` **1.000** instead of 0.415 — the deepest blue right beside a white core (**row 26's own colour walk, nearly broken by its width fix**). ⚠️ 11a's 34 offsets were re-derived from the RIGS in the same PR and **three were wrong**: the cape from her BoatDef's 12.9 m where her rig lofts 12.8 (6.45 → **6.40**), and both zodiacs from their label length over the TUBES where the rig lofts the hull (3.64 → **3.50**, 3.33 → **3.20**). Every rig does put its origin amidships, so LOA/2 was the right rule — applied to the wrong length three times |
| **34** | owner, 2026-09-08 in play — ***"the water starts oscillating very quickly when it changes states from light to calm to moderate etc, its like its speed resynches and it vibrates and its very noticable"*** | not a plate: arithmetic (`WaveStateChangeVibrationTests`). `WaveMath.Sample` forms `phase = k·x − ω·t` with **ω = √(2πg/λ)** and **λ a function of the wind**, so a mood change moves the phase at a fixed point by **Δω·t**. The drawn rate through a transition is `ω + t·dω/dt`, and the second term is the whole defect: **a frequency that is CURRENT multiplied by a time that is TOTAL**. | Measured on a light→blow change (4 s), against the wave's own **0.316 Hz**: **1 min → 30 Hz (96×) · 10 min → 311 Hz · 1 hour → 1 867 Hz · 3 hours → 5 604 Hz (17 715×) · 1 day → 44 839 Hz.** ⭐ **It grows with elapsed game time**, which is the signature — no wave, blend curve or frame-rate artefact cares how long you have been playing. **Dead control:** with the wind held STILL the shipped law is exact at every game time, so the sea is not drawn wrong, **only the CHANGE is.** | **high — P1.** The sea's moods are the feature and the transition IS the mood changing | no — a defect, not a look call | **PROPOSED: hold the frequencies FIXED and let sea state move only the AMPLITUDES.** Physically what a growing sea does (energy into frequencies that were always there, not existing waves sliding up the scale); the 8-train JONSWAP spectrum already has the bins. Measured: **5 604 Hz → 0.316 Hz, the wave's own rate exactly**, at every game time. ⚠️ Rule 5 is NOT the cause and must not be 'fixed' with an accumulator — the fix stays a pure function of (seed, gameTime), no state, nothing saved. §44.1 <br><br>✅ **SHIPPED (water PR B, 2026-09-09).** The bins are an absolute geometric ladder (`WaveSpectrum.BinWavelengthMeters`) with no wind term; the wind enters only in the ratio `ω_i/ω_p(U)` the JONSWAP shape is read at, so **the peak walks across bins that stand still**. Measured on the shipped config across a light→blow step: |Δφ| per bin was **27 008–47 486 rad at 3 h** and **270 080–474 858 rad at 30 h**; it is now **0 at every bin at every age**, exactly rather than approximately. Ladder in `GameConfig.asset`: **8 bins, 5–30 m**. Guard: `WaveFixedBinLadderTests` (10 tests, no GPU). <br>⚠️ **The 5 m short end was MEASURED, not chosen.** A wider ladder spaces the bins further apart in frequency and the sea stops grouping: against `WaveSpectrumTests`' own run-length metric (bar 1.84) 3–30 m scores **1.71 — a fail** and 4–30 m 1.66; 5–30 m scores 2.12. The price of 5 m is that below ~2.5 m/s of wind the fetch peak is shorter than the shortest bin, so a near-calm sea is drawn a little long — at the right HEIGHT (the peak is pinned to the ladder rather than falling off it; without that pin the sea lost 65 % of its height at 0.5 m/s, measured). More coverage needs more bins, which costs per-pixel trig and, past 8, the shader globals and the bridge. <br>⚠⚠ **A CORRECTION TO THIS ROW'S OWN PREMISE, found while fixing it.** The row says the DRAWN water vibrates. The drawn path was already mitigated: `WaveFieldBridge` publishes through `WaveFieldAnimator`, which advances phase INCREMENTALLY (`Φ += k·c·dt`) and bakes it into `PhaseOffset`, and the shader reads that baked phase rather than `ω·gameTime`. The defect was live on the **pure closed form** — the ADR 0018 sim reference, whose game-time callers are `BoatController` (the hull's ride) and `BreakerMath` (the bore's birth). So it is **not established that this fix is what the owner saw**, and this lane cannot establish it without the editor. What it does buy beyond the sim: with the bins standing still the closed form is phase-continuous by itself, so the accumulator is no longer what makes it so — which removes the reason the drawn sea and the ridden sea travel two different paths (P1's SEE == FEEL). Retiring the animator's accumulation is NOT in this PR; it becomes possible — **recorded by the lead architect 2026-09-09 as PR E, after D**. <br>⭐ **The likely reading, and the guard that pins it:** the DRAWN sea was continuous by accumulation; the **RIDDEN** sea was not; **the player saw the boat.** `BoatController` samples the closed form at the game clock and the camera rides the hull, so a mood change lurched the whole picture against water that was itself smooth — which at the helm reads exactly as *"the water starts oscillating"*. `TheRIDDENSea_IsContinuousAcrossEveryMoodChange_WhichIsWhatTheHullAndCameraFeel` bounds it: a wind step may now move the surface under the hull by at most the **amplitude difference** (0.5795 m light→blow), a bound that holds only because every train is at the same phase either side of the step; with the bins following the wind the two samples sat at unrelated points of the wave and the step could reach the sum of **both envelopes** (0.7661 m). ⚠️ Still not proof of cause — the helm settles that, and the plate is owed. <br>⚠️ **A correction to this row's ladder note (2026-09-09, same PR):** "5–30 m is the widest ladder that still groups" was **wrong**. Measured across ten ladders the run-length metric is **not monotone in the spacing** — 6–24 m is TIGHTER than 5–30 m and fails (1.70), while 3–50 m is the widest tried and scores best (3.44). The run length depends on where the bins fall relative to the PEAK at the condition measured. 5–30 m is a ladder that passes, not an optimum; whoever retunes it (PR D will) must re-measure rather than reason from the spacing formula. |
| **35** | owner, 2026-09-08 — at 13:12 ***"the vertical white water details vs the horizontal from nw to se water details… it just still doesnt feel like one coherent surface, it feels like layers"***; at 15:51, same beach, sun low, ***"it actually looks really good in the evening, its making me rethink what i just said"*** | ⭐ **Same water, two readings, and what changed is computable.** The swell's ONLY two shading terms (`_SwellFaceShade`, `_SunSideStrength`) are both multiplied by `swellReadGate`, and the sun side by `elevGate = smoothstep(0,0.12,e)·(1−e²)²` — which is **exactly 0 at solar noon** (PR 9's design, row 11). Sunrise 6 / sunset 20 puts solar noon at **13:00**, so **he was twelve minutes from the one moment in the day when the swell's colour shading is switched off**: gate **0.00000 at 13:12 against 0.12688 at 15:51** (validated against row 11's measured 0.374 at 17:00 / 0.0025 at 12:00). In light-to-moderate airs `swellReadGate` is low too (row 25), so BOTH terms were near zero at once. | ⭐ **This reframes the row: at noon the swell is a nearly FLAT colour field and every white layer is composited over it — the defect is the ABSENCE of a surface cue, not the presence of a bad one.** Same family as rows 6 and 25. ⚠️ **What layer he is seeing is NOT settled and must not be assumed** (I cannot see the screenshots; his own words read as *upright vs lying flat* at least as plausibly as *screen-vertical*). Live white candidates at his sea state: **`_DriftLineStrength 0.35`** (near-white, stretched 5×, alive ONLY between sea state 0.05–0.6 — his exact band — running along `_FlowDir` = the hard-coded **(1,0)**, a different direction from the swell) and the **`_WhitecapTex` stamp** (row 7's painted mark — a sprite by construction). ⚠️ **`_DriftLineFoamDrift` is ABSENT from all nine materials**, so it stamps 0 and the lines run on the raw current while the foam they are made of runs on the shared blend — the shader's own comment already says so. | ✅ **RESOLVED 2026-09-08 — and it is ROW 25.** Weather measured at his own clock: **13:12 sea state 0.181, 15:51 sea state 0.341**, and `swellReadGate = smoothstep(0.28, 0.45, sea)` reads **0.000 → 0.294** across them. ⚠️ **The solar-noon gate this row first blamed was REDUNDANT** — it is 0 at 13:12, but the swell-read gate was ALREADY 0 and multiplies the sun side too, so the swell had no shading whatever the sun did. What changed between his two readings is the **WIND** (0.90 → 2.77 m/s). ⚠️ **And it was NOT raining** — `RainEmitter` needs sea > 0.30 AND vis < 0.65; at 13:12 it was 0.181 / 0.806, nowhere near. The precipitation lead is withdrawn. | folded into **row 25** — the onset is the one knob, and row 25 already predicted this in writing | ✅ **SHIPPED with row 25 (water PR A, 2026-09-09)** at onset **0.10**: his 13:12 moment goes 0.000 → **0.136** and his 15:51 acceptance 0.294 → **0.769**. The onset table is §45 |
| **33** | (measured 2026-09-08 answering the owed AMPLITUDE row) — **the HEIGHT never followed the LENGTH, and it is the sea that is too TALL, not too flat** | not a plate: arithmetic over the shipped settings (`WaveAmplitudeMeasurementTests`). The owed row said *"1.5 m shipped vs ~3.6 m real at a gale, so the waves read flat."* ⚠️ **Both halves are wrong the same way.** (a) A gale HERE is **11–14 m/s** (`WeatherModel.SeaBandEdges`, Storm at 14); a real Beaufort 9 is 20–24, so the wind that justifies 3.6 m is never blown. (b) 3.6 m is the **fully-developed** PM height, and §39 already refused PM for the WAVELENGTH because this setting has 25 km against PM's 58–298 — taking PM's HEIGHT onto a JONSWAP LENGTH is that same refusal ignored. | Measured at a gale (U 12.5, peak 26.6 m): **drawn Hs 2.36 m vs 1.01 m that 25 km of fetch can raise** — steepness **0.089 against 0.038**, i.e. **2.3× too tall and 2.3× too steep**. ⭐ And the companion finding, which is worse: **#762 changed the length and left the height**, so at LIGHT AIRS the peak fell 8.4 → 2.2 m and the steepness went **0.048 → 0.182 — past the ~0.14 breaking limit.** A sea that steep cannot stand, and the hull rides the field the shader draws (ADR 0018). From a blow up nothing moved (1.07×, steepness within 0.01) — that half of the helm-feel verdict is *he should feel no difference*. | **high** — it is a sim defect, not a look one, and this lane shipped it in #762 without proposing the height half | ⭐ **YES, and it is a real choice**: leave it (the sea is stylised and he likes it), bring `PrimaryAmplitude` down to what 25 km raises, or **derive the height from the fetch the way the length now is** so the two halves cannot drift apart again | nothing moved. Tables in §43.2 / §43.3. The derived option is the one this lane recommends: it is the same JONSWAP curve the wavelength already uses, so it costs one function and removes a whole class of drift <br><br>⚠⚠ **THE STEEPNESS HALF OF THIS ROW IS WITHDRAWN (2026-09-09, water PR B; rule 6, nothing moved).** The measurement that produced it was taken on a sea nobody sails. `WaveAmplitudeMeasurementTests` built `WaveFieldSettings.Default` and patched only `SeaFetchKilometres`, missing the three other fields `GameConfig.asset` overrides — `SeaStateAmplitudeExponent` 1.35 vs **1.5**, `CrestSharpening` 2.2 vs **2.6**, and decisively `SpectrumBlend` 0 vs **0.65**, which is the difference between the hand-authored FOUR-train field and the eight-bin spectral one. Re-measured through the real `WaveMath` on the shipped config: **steepness H/λ never exceeds 0.105**, and that only at 0.5 m/s where Hs is 2 cm — the pinned **0.182 "past the ~0.14 breaking limit"** is an artefact, kept here beside the new number as the record. <br>✅ **THE ROW SURVIVES ON ITS OTHER HALF, and it is the stronger claim: the sea is too TALL.** Drawn Hs against the JONSWAP fetch-limited reference at 25 km — 0.98× at 1.63 m/s, **1.32× at 3 m/s**, **1.83× at 5.70 m/s** (the top of the reachable wind), 2.42× at a gale. The owner's "height from fetch" ruling stands on that half. <br>Same defect in row 30's fixture, independently: `WaveSpeedMeasurementTests` read bare `.Default`, i.e. `SeaFetchKilometres = 0`, so its "shipped peak" was the **legacy linear line** — 8.45 m against the fetch law's 2.21 m at 1.63 m/s. Its `TheWindToWavelengthLaw_IsLinear…` guard had gone on asserting a law #762 replaced, and passed. PR B fixes all three fixtures onto one mirror (`ShippedWaveField`) with a guard that walks the asset's own keys <br><br>✅ **SHIPPED (water PR C, 2026-09-09).** The height now comes from the same fetch-limited JONSWAP family as the wavelength: `g·Hs/U² = 0.0016·√(g·X/U²)`, capped by the fully-developed Pierson—Moskowitz ceiling `Hs = 0.0246·U²`. A law with tunables (`FetchHeightCoefficient`, `FullyDevelopedHeightCoefficient`, `HeightStyleScale`, `GlassGateSeaState`), not a table, so the two halves cannot drift apart again. **drawn/real goes 1.59× → 1.00× at 2 m/s, 1.35× → 1.00× at 4 m/s, 1.69× → 1.00× at 5.70 m/s** — the drawn sea IS its own reference now, not merely near it. <br>Guard: `WaveHeightFromFetchTests` (8 tests, no GPU) — the law's identity at every wind, steepness under the 0.14 breaking limit **including the gale/storm rows PR D will unlock** (worst reachable 0.0295, worst at 25 m/s 0.0505), monotone in wind, zero sea at zero fetch, glass bit-exact 0 at any wind, the lee envelope NOT folded into `TotalAmplitude`, and the superseded pair still reachable. <br>⚠️ **The height is derived at UNIT amplitude and set on the FINISHED field.** The obvious shortcut — convert the target Hs to a primary amplitude via the secondaries' ratios — is wrong whenever the spectrum is on: the spectrum preserves the ENVELOPE (Σa) while spreading it over eight bins, and Hs goes as √Σa², which spreading reduces. Written that way the sea came out at **0.67× its own reference** — measured, then fixed. <br>⚠️ **THIS ROW MOVES TWO CALIBRATED NUMBERS and the owner should see them at the helm.** `TotalAmplitude` is the whitecap crest-factor normalizer AND the watertight hull clamp's bound. Against the superseded sea it **drops 0.664 → 0.393 m at 5.70 m/s** (the sea is simply shorter); and with the height law on it is **Hs**, not the envelope, that the spectrum blend conserves, so across the blend the envelope **rises 1.44×** at a fixed height. Neither is a defect — they are what "the height comes from the fetch" means — but foam scoring and how high every hull sits both read that number. <br>⚠️ **`WaveFieldSettings.Default` keeps `HeightFromFetch = false`** (ADR 0027's passthrough discipline): the reference tuning is what 129 pre-2026-09-09 assets deserialize to and what the pinned-ULP and passthrough guards compare against, so this PR moves the SHIPPED sea without moving a single frozen baseline. The ruling lives in `GameConfig.asset`, which is authoritative (#600). <br>⭐ **The steepness assertions are a RAIL, not evidence of the withdrawn defect.** On the shipped config the superseded steepness never exceeded 0.105 and never breached 0.14 (see the withdrawal above); the guard exists so the two halves cannot come apart again, especially once PR D unlocks the gale. <br>⚠️⚠️ **SUPERSEDED IN PART BY PR D (2026-09-09).** The floor quoted here (wind never below 0.50 m/s, sea state never below 0.143) was true of the wind law as it stood, and it was the argument this row's ruling was priced against. **PR D widened that law**, so the floor is now **0.000 m/s / sea state 0.000** — a glass calm is reachable at last. ⭐ **The ruling survives and is better for it:** at the new floor the swell-read gate is **0.0000**, and at a true Glass sea (0.25 m/s, sea state 0.071) it is **0.0000** as well, so the 0.041 this row priced as the cost to row 5's mirror is **no longer paid at all** — the mirror is now both reachable and unshaded. The cap itself never bound: see row 36. |
| **32** | (found while shipping rows 9+10) — **the PAINTED seabed is still eight bits** | not a plate: read out of source. ADR 0014's painted path hands the shader an imported PNG (`PaintedSeabed_HeightTex.png`) whose bytes `PaintedTidalTerrain` also decodes — render == sim by construction, which is the point of it. But it is 8-bit, so it carries **exactly** the defect row 9 just measured out of the baked path: at St Peters' own spring low the same half-code becomes the same two thirds of a metre of drawn edge. `WaterSurface.FeedPaintedHeightTexture` is untouched by the R16 change because the bytes are not ours to widen. | Latent: **medium the day a painted map is adopted, none before it.** St Peters is the arrival region, which is what makes the trap expensive if it springs. | low now, medium on adoption | no — same defect, different source | its own row — and **smaller than first written** (corrected before this PR merged). The DECODER needs nothing: `PaintedHeightField.DecodeElevation(float r01, min, max)` takes a NORMALIZED FLOAT and `GetPixels()` returns floats whatever the bit depth, so both the sim and the paint tool already read any width. What is 8-bit is the WRITE: `TerrainPaintTool` builds `new Texture2D(..., TextureFormat.R8, ...)` at **two** sites — the paint tool's PNG export (~line 1018) and `BakeStPetersSeabed` (~line 1730) — and `EncodeElevationPixels` hands them full floats that only lose precision there. ⚠️ **NOT LIVE TODAY — this row is a TRAP, not a symptom, and the first two versions of it said otherwise.** `StPetersSeabed_HeightTex.png` and `NineMileCreekSeabed_HeightTex.png` ARE committed (an earlier check grepped for *painted* in the filename; they are named by REGION). But **neither is wired into a scene**: both `NineMileCreek.unity` and `StPeters.unity` carry `_depthSource: 0` (Auto) with `_paintedHeightTex: {fileID: 0}`, and their sims are the ANALYTIC `MainlandTidalTerrain` / `TidalTerrain` — no `PaintedTidalTerrain` in either (checked by script GUID). **So both regions render through the BAKED path, which row 9's R16 fix does reach.** What makes this a row anyway: `TerrainPaintTool.AdoptOnOpenScene` is a shipped workflow, and the owner has an untracked `PaintedSeabed.asset` in his own checkout — so **the day he adopts a painted seabed, St Peters silently drops from sixteen bits back to eight.** A good change quietly undoing a fix is the defect here. ⚠️ **The trap to guard**: the texture IMPORTER can silently down-convert the PNG back to R8 with every test still green — the same shape as the `SetPixels32` trap in row 9's fix. So the acceptance must assert the format Unity **LOADS**, not the one the tool wrote, and that guard needs an editor |
| **31** | (found while opening rows 9+10) — **a region's seabed-resolution intent is clamped in SILENCE, and the two caps disagree** | not a plate: read out of source. `NineMileCreekMainland.WaterHeightBakeResolution` derives `ceil(760 m x SeabedPixelsPerMetre 2) = 1520` texels and St Peters the same; `WaterSceneTemplate.ConfigureLandRegionWater` then does **`Mathf.Clamp(resolution, 16, 256)`**, matching `WaterSurface._heightResolution`'s `[Range(16, 256)]`. Meanwhile `RegionDef.MaxSeabedTexels` is **4096**. So a region computes its own seabed grid from a stated pixels-per-metre, and the water surface throws 83 % of it away without a word. | The shipped seabed height map is therefore **256² over a 760 m region — a 2.97 m texel** — where the region asked for 0.50 m. Two caps in two files disagree by 16x, and the derived value that loses is the one carrying the region's own intent. It is `R8` and `FilterMode.Bilinear`, so the VALUE quantum (3.92-4.71 cm, i.e. 0.09 m of shore at slope 0.5 and 2.35 m at 0.02) is sub-texel at every plausible slope — **the pitch, not the bit depth, is what a 1-3 m feature can come from**. | medium on its own; ⚠️ **NO LONGER the leading candidate for row 9** — measured out 2026-09-07, see the fix cell | no — it is a defect, not a look call | ⚠️ **PARTLY OVERTURNED 2026-09-07 — and the sentence that was wrong is the last one.** Measured (`SeabedBakeCombTests`): the pitch is **not** row 9's cause. At spring low the shipped 256 grid draws an edge 28.7 cm ragged; the SAME grid with exact values draws it **1.2 cm** ragged, and 1520 texels at R8 still leaves **13.5 cm**. The retirement of the 16-bit map was reasoned from *"a quantum already sub-texel"* — **sub-texel in ELEVATION, which is not the units the player sees.** 4.71 cm of elevation is 1.35 m of GROUND on the 0.035 shelf spring low bares, and this row's own note computed that figure (*"2.35 m at 0.02"*) and then dismissed it anyway by assuming a steep shore. **R16 is reinstated and is row 9's fix.** This row stands on its own merits — a stated intent silently discarded, and two caps disagreeing 16× — but it is **no longer the leading candidate for row 9** and must not be sold as one. Its own benefit, measured: R8 256 → R8 1520 takes 28.7 cm to 13.5 cm (real, but half a fix at 24× the memory). |
| **29** | *"the foam seemed off-centred with three different sections leaving the boat"* (2026-09-06, in play) — **three foam families left the hull from three different points, at two different widths, along two different tracks** | his three night screenshots; then measured out of source and re-measured as arithmetic in `OneWakeRootTests` | The buffer's sheet took the RIG-lofted `WakeSternOffsetMeters` (6.40 m on the cape) while the sprite deposits and crest lines took `BoatHullDef.LengthMeters`/2 + nudges (6.60) — **the same 12.9-vs-12.8 disagreement PR 11b fixed on ONE side**. Widths disagreed too (beam 2.4 m vs `length × 0.14` = 1.81, **0.75×**), and the deposits rode `Lerp(travel, sternSwept, 0.25)` — three quarters of the ORIGIN's arc — where the buffer's capsule is laid transom-to-transom. ⚠️ **Foreshortening was the obvious suspect and is REFUTED**: all three resolve the same `ElevationDeg` through the presenter seam. | ✅ **DELIVERED** (was high) | no | **Row 29 PR** (`feat/water-row-29-one-wake`) — `water-rendering.md` §40. `WakeRootMath` in **Core** is the one root (one `ForeshortenY`, one projection, one `SternWorld`); `FoamBuffer.SternWorld` and `WakeGrading` become DELEGATIONS so 11a/11b's guards read unchanged. The rig facts reach the emitter through `IBoatHullPresenter` exactly as the bake elevation already did — a mesh hull answers from her def, a sprite hull answers 0 and keeps the LOA/2 and length-fraction rules, **so the hand-drawn fleet is unchanged**. All 34 hull defs carry a lofted offset. ⚠️ **THE TRACK NEEDED NO FIX, and that is the row's second result.** The charter read the deposits' `Lerp(travel, sternSwept, 0.25)` as a positional disagreement; it is not one. Deposits land at `PointOnTrack(prevStern, stern, t)` — the transom's own swept path, the same segment the buffer's capsule lays on: measured through a turn at every fraction, **0.0000000 m** apart. The fraction steers `trackDir`, the shoulders' LATERAL AXIS, and setting it to 1 broke `ShippedSwingFraction_PullsTheTrackBackOntoTheCourse_ButKeepsSomeKick`, which pins a deliberate anti-fanning decision. **That guard was right; 0.25 stands.** The number that suggested otherwise was an ANGLE reported as a lateral gap — my error, corrected. Widths: the cape's lobes 1.81 → 2.40 m, the churn keeping its 0.714 shape ratio. No-op on a straight run (1e-6 m/step). ⚠️ NOT fixed: `WakeParticleSystem`'s flat 0.5 m unprojected apex — 11 test references, ZERO production callers; named rather than "fixed", because fixing it changes nothing he sees. |
| **28** | **the foam buffer does not FADE at 60 fps** — its decay is quantized away, and the better the machine the more permanent the wake | arithmetic on the shipped pass, shot beside PR 11b's own numbers (`FoamBufferQuantizationTests` reports the table); it is the second half of the owner's 2026-09-06 sentence, *"it doesnt widen over time **and fade away**"* | The buffer is `RenderTextureFormat.RG16` — **8 bits per channel**. The advect pass multiplies the previous value by its decay factor, adds the frame's deposits and writes the result back into those 8 bits, so a per-frame change below **half a code (1/510)** rounds back to where it started. The decay is a MULTIPLY, so its change is largest at full white: coverage (6 s half-life) loses **0.491 codes/frame at 60 fps — under the floor at EVERY stored value**, so the channel does not decay at all; freshness (4 s) loses 0.735 and **stalls at stored 0.680**, two thirds of the way down the colour walk. At 30 fps both move; at 120 fps and above neither does. The wake still leaves the 96 m window and the compose's lace still tears at it, which is why this has hidden for so long. | ✅ **DELIVERED** (was high — half of a sentence the owner has now said twice) | **a smaller one than it looked.** The FORMAT question was settled by MEASUREMENT, not taste (§36.4: the dither gets the mean right and then loses the dying rim to its own random walk, and an exact error diffusion costs the same two bytes a texel the wider format does). What is left for him is the **half-life**, which had never done anything at 60 fps and is therefore an untested value rather than a tuned one — §36.5 tables it against speed. **No dial was moved** | **PR 11c** (`feat/foam-buffer-fade`, Tier A) — `docs/design/water-rendering.md` §36 carries the derivation. The format is CHOSEN by `FoamBuffer.SelectFormat` against `SystemInfo.SupportsRenderTextureFormat` from a three-rung preference (`RG32` → `RGHalf` → the shipped `RG16`, that last one NAMED as the stall rather than silently reintroduced), and **no format literal is typed at the descriptor any more** — a source tripwire keeps it that way, because a constant typed there is the exact shape this defect shipped in. `RG32` over `RGHalf` is measured, not preferred: the half float's mantissa grid is CONSTANT across an octave while the decay's change is PROPORTIONAL to the value, so it carries a frame-rate-dependent bias (a texel at 1.0 reads 0.5347 after one half-life at 144 fps against `RG32`'s 0.50001). MEASURED: a texel at 1.0 now reaches half within one half-life ±1 frame at 30/60/120/144 fps in BOTH channels, where the shipped target failed all eight arms; the coverage decay comes to rest at **0.00395** instead of **1.000**; the freshness clock walks to `age01` 0.997 / ramp **1.000** across 2016 stored values instead of stalling at 0.322 / ramp 0.305, so #724's shallow blue was previously unreachable. Cost **+2.25 MB per camera** (2.25 → 4.50 MB at the shipped 96 m window), stated and guarded. ⚠️ **The old plate harness could not have caught this**: `FoamTrailRootPlateTests` ping-pongs the advect pass through `ARGBFloat`, which has no 8-bit rounding in it — the defect lives in the STORE, so `FoamFadePlateTests` makes the two real formats the arms |
| **30** | *"waves seem to move across the screen too fast, should be realistic to actual waves and windspeed/conditions"* (2026-09-06 evening, in play) — **the sea's crests arrive about twice as often as a real sea's in the same wind, because the wind→wavelength law is LINEAR where a real one is QUADRATIC** | measured, not plated: `WaveSpeedMeasurementTests` reports the whole table from the shipped `GameConfig` values and the shipped per-hull camera framings | 🔴 **The charter's hypothesis is REFUTED, twice.** It proposed that the drawn wavelength is scaled while the phase advance is not, so a drawn crest would run at 2.8× the speed its drawn length allows. (a) `_OceanSwellScale` ships at **0.025** and the shader normalises by that same 0.025, so the visual frequency scale is **exactly 1** — the drawn wave IS the modelled wave, and water PR 6 closed this in 2026-09-02. (b) Even at the old 0.07 the sign ran the other way: the ratio is `1/√fs` (the drawn speed falls as `c/fs`, the allowed speed only as `c/√fs`), so fs 2.8 gave **0.60×** — too SLOW for its drawn size. <br>**What IS wrong is the wind coupling.** `lambda_p = 6 + 1.5·U` (capped 40 m) against Pierson–Moskowitz's `≈ 0.833·U²`. A line and a parabola cross once — at **U = 3.74 m/s** — so the shipped sea is too LONG below that and too SHORT above it: **0.54× at a blow, 0.18× at a gale, 0.11× at 20 m/s**. Because `T = √(2πλ/g)`, short waves mean **crests arrive 1.4× too often at a blow and 2.3× at a gale** — frame-independent, and the reading of "too fast" no camera can fix. `SpectrumBlend` (0.65 shipped) does not move this: `SpectrumTrainsFrom` is handed the same peak and spreads slots around it. | high — it is P1, and it is the owner's own sentence | 🔴 **YES, and the ask contains a CONFLICT he has to settle.** "Not so fast across the screen" and "realistic to windspeed" pull in OPPOSITE directions, because `c ∝ √λ`: making the sea realistic for its wind LENGTHENS it, which speeds each crest across the frame (a blow on the cape: crossing 10.9 s → **8.0 s**) even as it slows the rate crests arrive (3.05 s → 4.16 s). The three levers, measured: **(i)** the PM law — right periods, faster crossings; **(ii)** shorter waves — slower crossings but crests arrive MORE often, i.e. worse on the frame-independent number; **(iii)** ZOOM OUT — the only lever that slows the screen without touching dispersion (32 m framing: 10.9 → 14.5 s; 48 m: 21.8 s), and it is a camera call with its own cost. ⚠️ Also his: the frame is PER HULL (`CameraWorldHeightMeters` — dory 14 m, cape 24 m, packet 90 m), so the same sea already reads ~1.7× faster on a dory than on a cape. | **NOT BUILT** — this row is the measurement PR (`feat/wave-speed-measurement`), no knob moved, no shader touched. The build waits on his ranking of (i)/(ii)/(iii). ⚠️ If (i) is chosen, `WaveMath.TrainsFrom`'s length law is a SIM change, not a look one: the hull ride samples the same field (ADR 0018's one-sea rule), so a longer swell moves the seakeeping too and row 6's ride≠drawn question closes with it. |
| **27** | **the wake does not lift real water — nothing rides another boat's wash, or its own** | asked by the owner 2026-09-06: *"is the v wake dynamic? Does it cause real waves … Interacting with other waves?"* | Answered from the code, and it is deliberate and documented: the wake wave carries a real amplitude in METRES born from the hull's size/weight/speed grade, lifts and sizes its own drawn crest, and collapses over its life — but `WakeWaveMath` states outright that it "displaces the wake's OWN drawn water and leaves the sea mesh — and the clamp that reads it — untouched", because lift the hull's watertight clamp cannot see is the `_OceanSwellScale` defect class this repo has already paid for once. So `BoatWaveMotion` samples the ocean field only. Separately: the ocean's own trains DO interact — `WaveFieldSample` sums up to 8 of them, heights AND slopes, which is real interference — but it is LINEAR superposition: no energy passes between trains, waves do not steepen each other, and breaking is depth-driven. Wake waves sit outside that field entirely. | medium-high (it is most of what "dynamic wake" means once the trail reads right) | **yes — the owner ranks this against rows 9/10/8** | NOT PR 11, fenced deliberately. Shape: the wake's crests join the sampled field (or a second field the clamp also reads) so `DisplacedWaterMath.WatertightZHeaveMeters` and the hull ride see them — hull clamp and wake field in ONE change, or it repeats the defect it is avoiding. Tier B |
| **36** | owner, 2026-09-09, on being told the first gale would be about a metre: ***"thats ok"*** — **the sea-state ladder was unreachable: five of its eight rungs never happened** | measured over a game week × nine seeds on the shipped `WindProfile.CoddleCove`: wind **0.58—5.33 m/s**, and the hours fell **Glass 0.0 % / Calm 15.3 / Light 71.0 / Moderate 13.7 / Lively 0.0 / Rough 0.0 / Gale 0.0 / Storm 0.0**. A player could sail for a week and meet three moods. | ⚠️⚠️ **THE RULING WAS PHRASED AS REMOVING A CAP, AND THE CAP WAS NEVER THE PROBLEM.** The law was `MeanStrength 3 + strN·1.3 + gustN·1.2` with both noises in [−1, 1], so it topped out at **5.5 m/s against a `CalmMaxStrength` of 5.7** — the clamp could not be reached, let alone bind, and deleting it would have changed **nothing**. The ceiling was the law's own amplitudes. (The same 0.50/0.143 floor is quoted as settled fact in rows 25 and 33; both now carry the correction.) | the ruling itself | ✅ **SHIPPED (water PR D, 2026-09-09).** A gale is not a big gust, it is a **weather system**, so the strength gained a third channel: much slower than the swell and deliberately skewed (`max(0, n)^SystemShape`) so it rests at zero most of the time and occasionally builds for hours. Scaling the mean instead would have needed ~7.5 m/s and put the AVERAGE day at Lively — permanently rough, which trades P5 for the feature. The mean came **down** (3 → 2.2) because the old floor sat exactly on the Glass/Calm edge, so a mirror was as unreachable as a gale. <br>**After, same sweep: Glass 6.3 / Calm 24.9 / Light 40.0 / Moderate 12.5 / Lively 5.7 / Rough 6.1 / Gale 4.3 / Storm 0.3** — every rung reached, Calm+Light+Moderate still **77.4 %** of the week, the longest unbroken Gale-or-worse spell **14.2 h**, and **6 of 9 worlds** see a gale inside a game week (the owner's own seed 12345 among them). <br>**Per-region wind is DATA now** (rule 2): `WindProfileDef` in Core, one asset per region, `RegionDef` references it, an unwired reference falls back to `WindProfile.CoddleCove` **by name** and never to a zero profile — a zeroed struct clamps every wind to nothing and is a dead calm forever, not a gentle one. ⚠️ **The weather SYSTEM is a WORLD fact**: one noise stream and one shape per world seed, shared by every region, with only its STRENGTH local. Otherwise Nine Mile Creek blows a gale while St Peters, ten kilometres away on the same island, lies glass. Guarded exactly (`TheWeatherSystem_IsAWorldFact_...`). <br>⭐ **What the uncap turns ON, which is worth the owner's eye:** every hull carries a `MaxSafeSeaState` (dory Lively, skiffs Rough, cape/lobster Gale) and the weather could never exceed Moderate — so **every boat's seaworthiness limit has been unreachable since it was authored**. It can now be exceeded on all of them. ⚠️ But `MaxSafeSeaState` is **read by nothing** — the field, its inspector and two doc comments calling it "a gameplay gate" are its only references. It is a dead knob, and making the weather reach it does not make it do anything. That is gameplay-systems work, not the water lane's, and it is filed here rather than fixed. (The storm-rock blend, gated at `StormStartSeaState01` 0.4, WAS already reachable — the old law's peak was sea state 0.524 — so that one was live.) <br>Guards: `WindUncapReachTests` (the hours-per-rung acceptance, the episode shape, the determinism, and a dead control proving the superseded law reached only 3 of 8 rungs and never touched its cap) and `WindProfileDataTests` (the world-fact guard, the assets' key coverage, the fallback-by-name). `WindFieldTests.Wind_StaysInCalmBand` is re-derived: it asserted `SeaFromWind ≤ Moderate`, which is the thing the owner ruled away. |
| **37** | found while shipping row 34 (2026-09-09) — **the DRAWN sea was not a function of `(worldSeed, gameTime)`; it was a function of the frame sequence** | not a plate: read out of source, then measured. `WaveFieldAnimator` advanced each train's travel phase incrementally (`Φ += k·c·dt`) and baked it into `PhaseOffset`. Its own class doc confessed the cost: *"it is NOT a pure function of gameTime: two machines running different frame rates ease and accumulate along different paths, and a save/load does not reproduce its state."* Measured over ten minutes of game time, 60 fps and 30 fps ended **0.003 rad apart** on the peak bin — small, and growing with session length. | ⚠️ **A rule-5 leak in the thing the player looks at**, and the reason the drawn sea and the ridden sea travelled two different code paths at all: the sim reads the closed form, the pixels read an accumulator, and P1's SEE == FEEL rested on the two happening to agree. | no — a defect, and the lead architect recorded the decision 2026-09-09 | ✅ **SHIPPED (water PR E, 2026-09-09).** The travel phase is now the closed form `Φ(t) = wrap(ω·t)` evaluated at the game clock, accumulated in double and wrapped before it drops to float — exactly what `WaveMath.Sample` does, and for the same reason (ω·t reaches millions of radians in a long session; float32 would lose the wave). <br>⭐ **The change is small because PR B earned it.** The accumulator existed because `ω` came from `λ_p(U)`, so every mood change moved it and `Δω·t` threw the sea across the wave (row 34: up to 474 858 radians at thirty hours). With the bins on a fixed ladder `ω` is constant and `ω·t` is continuous by itself — so the accumulator had stopped being what made the phase continuous and was only the leak. ⚠️ **That dependency is load-bearing and is asserted** (`ThisOnlyWorksBecauseTheBinsAreFIXED_...`): put the wind back into the wavelength and the drawn sea is discontinuous again. <br>**The shader does not change at all.** It still reads the baked `_WavePhases` and adds its own look-back; only the SOURCE of that number moves, from a running total to a clock. `Tick` gains a `gameTimeSeconds` parameter and all seven call sites pass `WaveFieldAnimator.GameTimeSeconds` — deliberately **the same clock `BoatController` rides**, so the two paths are one function of one clock rather than two that agree. <br>Guard: `WaveFieldPhaseIsClosedFormTests` (6 tests) — two frame rates draw the same sea at the same game time; a **cold start** at any instant draws what an hour-long run would have (what a save/load needs); the drawn surface IS the ridden surface to 0.01 m; the baked phase is literally `wrap(φ_hash − ω·t)`; and a **dead control** shows the retired accumulator failing the frame-rate comparison, so none of the above is satisfied by a field that ignores time. <br>⚠️ **What is still stateful, stated rather than hidden:** the AMPLITUDE/wavelength/direction easing. A mood should arrive over a second and a half, not in a frame. It is presentation-only and it converges, so two frame rates reach the same sea by slightly different routes — which is why the see-equals-feel guard compares once the easing has settled. |

---

## 3. Owed verdicts and rulings (the owner's, in one place)

1. ~~**Rank this register** — PRs 4+ take his order.~~ **RANKED 2026-09-04: row 2, the foam language, is next** — ACTED ON in water-fidelity PR 8 (see row 2 and §33 of `water-rendering.md`). **Row 11 then shipped as water-fidelity PR 9** (see row 11 and §34 of `water-rendering.md`) — and shipping it opened **row 25**, which now gates JUDGING it: the sea is drawn near-black at every sea state where the modelled swell is awake, so the sun side has nowhere legible to land on open water. His order for what is left, so nobody re-asks: **row 25 first if he wants row 11 judged**, then **rows 9 + 10** (the comb wet edge, the ~1 m hairy deep/shallow wall) → **row 8** (the 4 m caustic patchwork). Rows 14 and 16–24 are carried: they need his eye in Play, not a plate. ⚠️ **Row 24 is new and it is PR 8's own**: the caps jump to the ramp's end instead of walking it, which in a GALE all but removes the milky residual — one dial (`_CapAgeStrength`) reverses it, and the gale row of `SHEET-foam.png` is where he rules on it.
2. ~~**Row 5**: what should a glass calm look like — a mirror, a stripe, a sheen?~~ **RULED 2026-09-02 ("glass calm, i trust your judgement for the game, but we need reflections on water") and ACTED ON in water-fidelity PR 5.** The lane's reading, delivered: a mirror has no WAVELENGTH, it has a SURFACE — the sheen now rides `1/(1 + k·|slope|)` off the shared field's own analytic tilt (`_MirrorForm`, `_MirrorSheen`, `_MirrorTiltScale`; 0 = the shipped stripe exactly). Row-band contrast 0.165 → 0.063 (open water) and 0.168 → 0.064 (sand) against a reflection-off control at 0.001 / 0.004, with the light unchanged by derivation: `_MirrorSheen` defaults to 0.3125 because that is what `(0.5+0.5·sin)³` averages. **Row 13 falls out with it** — chop IS tilt, so the mirror dissolves with the water instead of on a `_Chop` ramp. And the sun-streak glitter now rides the tilt's exact COMPLEMENT: with the rug gone it was a field of fine vertical glints over the whole calm frame, reading as rain. Sheet: `SHEET-mirror.png`. All reflected CONTENT (sky colour, clouds, the moon's disc and path, stars, object reflections) is untouched — that was never the complaint.
3. ~~**Row 6 / 15**: the `_OceanSwellScale` question — one sea at the field's true wavelengths (the visible waves 2.8× longer than today), or today's look with ride ≠ drawn kept?~~ **RULED 2026-09-02 ("swell scale you can make the changes for a longer slower swell, thats fine") and ACTED ON in water-fidelity PR 6, Tier B.** `_OceanSwellScale` 0.07 → 0.025 on all nine materials, so the `DisplacedSea` seam's frequency scale goes 2.8 → 1 and the drawn sea IS the field — same wavelengths, same periods, the same evaluation the hull rides (`AtTheShippedScale_TheDrawnSeaAndTheSIMSeaAreOneFunction`). **Row 15 closes with it.** Re-plated: the blow and gale columns go from a dense weave of short lanes to legible long crests with faces, which is the substance of row 6 — see below. `WaveFieldCostReport` identical before and after (15 call sites, 320 transcendentals/px, 373 780 B). ⚠️ `Water_StormGrey` had been hand-tuned to 0.04; that "a storm's swell is longer" is now inherited from the field's own spectrum instead of from a preset knob. **OWED: the owner's FEEL verdict at the helm** — he ruled the direction, not the feel.
4. ~~**Row 7**: the whitecap mark and the slot strength — repaint, retune, or let the procedural field place the caps.~~ **RULED 2026-09-02 ("let the procedural field replace the caps") and ACTED ON in water-fidelity PR 7.** `_WhitecapTexStrength` → **0** on all nine materials and on the shader default, so the procedural cap field places the caps and the painted sheet's 16 mirrored copies of one mark stop being what the sea wears. ⚠️ `Whitecaps.png` is NOT deleted and the slot is NOT removed — both stay reachable and 1 hands the caps straight back; this records which source SHIPS. All nine because the slot is MOOD-EASED (one preset left at 0.865 would return to the sheet whenever the weather leaned that way). Ridden alongside: `_CapAgeStrength` gives the caps the WAKE's own colour walk through the same knots — row 2's direction, not row 2 — built, gated and **shipped at 0**, because the effect is below this instrument's run-to-run noise (an isolated run read red:blue 0.62 vs 0.67 in a gale and 0.35 vs 0.42 in a blow; the full suite read 0.467 vs 0.470 — the direction survives, the magnitude does not, because _Time decides which crests are breaking and so which pixels are in the decile). A look change nobody can see is not one to ship. Sheet: `SHEET-caps.png`; the eye is the acceptance, and the coverage numbers are reported rather than asserted for the reasons §32 of `water-rendering.md` sets out. **Row 2 (the foam-language unification) inherits caps already born into the palette.**
5. ~~**Row 12**: the night level and the shipped searchlight against #691's plates.~~ **RULED 2026-09-02 and ACTED ON in water-fidelity PR 4:** *"It should be dark enough at night that the player feels the need to use radar and the lighting, a clear calm night with moonlight should be brighter if not cloudy."* Delivered as (a) `Resources/DayNightProfile.asset` SHIPPED, so the night is an inspector value at last; (b) `MoonlightLiftMax` 0.05 → 0.45, with the moonless/set-moon floor bitwise unmoved; (c) cloud now takes ALL of the moon's lift instead of 40 % of it (the old `1 - weatherDim` was capped at `WeatherDimMax` 0.6, so full overcast was never reachable); (d) `_BeamLitStrength`, an ADDITIVE lit-water term on the lamp's cone, because the shipped multiply-reveal cannot light a black sea. Measured over `night-*` and `lamp-*`: moonless 0.0096 mean wet luma / 0.0095 break-line contrast, clear full moon 0.1278 / 0.1315 (x13, x14); the shipped lamp's in-beam luma 0.051 -> 0.193 at glass, 0.057 -> 0.217 in a blow, the sea outside the cone unmoved. Sheets: `SHEET-night-corners.png`, `SHEET-night-lamp.png`. **This clears the gate on boat-lights PR 2.**
6. The check-ins PR 2 brings (in its PR body, shot by `CheckIn_TheSpillingBeachInBeats_AndThePlungingLedgeAsAnEvent` at 35 m with all three dials at 1): (a) the spilling beach in beats, (b) the plunging ledge as an event. Both are owed his eye; the dials ship at 0 until he turns them.
7. **The lip spray ships ON** — the one default in PR 2 that is not today's look, because a burst cannot be judged from a plate. A Play-mode look at the steep stretch (114, 157) in a blow. The dial is now **`GameConfig` ▸ `SurfSprayIntensityOffset`** (0 = the shipped burst, −1 silent, +1 double): PR 3 moved it out of the code at #699's review, because the emitter installs its own host at runtime and a value serialized there was a default the owner could never reach. Stored as an offset because a missing YAML key deserializes to ZERO, and for a plain intensity that zero would have switched the ruling off in silence.
8. **PR 3 ships the FEEL on while PR 2's LOOK dials are still at 0** — for exactly the reason above (a shove and a lift exist only at the helm; no plate can judge them). The consequence is worth his eye: until check-in 6 gets its nod, the hull BEATS and LIFTS to a bore the water does not yet DRAW. Turning the four `_Surf*` dials up is what puts the two back in step, and both are one nod away. The feel's own dials — `GameConfig.Seakeeping.SurfBorePulse01` and `SurfLiftScale` — are the other direction if he would rather the hull waited for the water.

9. ~~**Row 25 / 35**: lower the swell-read calm gate, or fix the darkness itself?~~ **THE GATE HALF IS RULED 2026-09-09 ("go with 0.10") and SHIPPED in water PR A.** `_SwellReadSeaStateLo` 0.28 → 0.10 on the shader default and explicitly on all nine water materials; `_SwellReadSeaStateHi` unmoved at 0.45. What it buys, at his own two readings of 2026-09-08: gate **0.000 → 0.136** at 13:12 ("it feels like layers") and **0.294 → 0.769** at 15:51 ("it actually looks really good"). What it costs, stated to him before he ruled and asserted in the guard so it cannot be quietly undone: a faint **0.041** read on the sea the price was QUOTED on (0.143). ⚠️ **That quote is stale, and the ruling cost him LESS than he was told.** #797's 0.143 was a measurement of the wind law *as it then stood*; **PR D (#811) widened that law** — `WeatherModel` clamps the wind strength at a literal `0f` lower bound (`WeatherModel.cs` ~90–92) and `WindUncapReachTests` **measures** a Glass sea on **6.3 %** of a game week over nine seeds (asserted `InRange(3.0, 8.0)`). At the true floor — sea state **0.000** — the gate reads exactly **0.000**, so row 5's mirror, on water that is actually glass, was never touched at all. Corrected inside the guard on 2026-09-10 (water PR B) so it cannot be re-quoted: `WaterSwellReadOnsetTests` now carries `TrueCalmFloorSincePrD = 0f` beside the `CalmSeaTheRulingWasPricedAt = 0.143f` it was priced on, and asserts BOTH — 0.0416 at 0.143, and exactly 0.000 at the floor. ⚠️ **Two things this does NOT settle.** (a) Row 25's OTHER half is still open: at a blow the drawn sea is near-black (mean wet luma 0.012 at noon), so the swell now has shading at sea states where the water is bright and still has nowhere legible to land at the sea the player sails in a blow. (b) The onset pair is shared with `SwashSeaStateGate()`, so the shore's calm fade moved with it — by design (one axis, one place to tune), and the knob for the shore alone is `_SwashCalmGate`.

10. ⭐ **Row 25 / row 6 — the sea goes black in a blow. Here is the bill for each way of fixing it. WHICH ONE?**
    *(asked 2026-09-10, water PR B. **Nothing has been moved.** This PR ships the measurement and the
    table and no knob change, because the answer may be a knob you already own and that is your call,
    not the lane's.)*

    At noon on open water at mean tide the sea reads **glass 0.331 → light airs 0.176 → a blow 0.012
    → a gale 0.021**. That is a **15× drop between a breeze and the sea you actually sail in**, and it
    is almost entirely one thing: the reflection. `_ReflectionFadeChop` sits at 0.6 on all nine materials
    and takes the mirror from 62 % of its glass value to **2 %** between light airs and a blow. Subtract
    the mirror and a blow's water is **0.009** — the body has almost nothing in it. There is no lighting
    bug to find; the sea is dark because the only bright thing on it has been switched off by chop.

    **Three ways to put light back**, priced at half-way back to a breeze (the blow 0.012 → 0.088).
    Each row is one change; the other columns are what that change would do to the seas you did NOT ask
    to move:

    | | what moves | glass | light | **blow** | gale | what it costs you |
    |---|---|---|---|---|---|---|
    | **(a)** | `_ReflectionFadeChop` 0.6 → **1.12** | 0.331 | 0.239 | **0.088** | 0.023 | **Nothing at a glass calm** — not by tuning, by arithmetic: at a dead calm there is no chop for this knob to fade, so your mirror cannot be touched by it at *any* value. Almost nothing at a gale either (0.021 → 0.023). What it costs is the **intent**: "a storm doesn't mirror". A blow would start to mirror. ⚠️ Needs the shader's `Range(0,1)` widened, and must **not** be applied to the sharpness curve — a storm that mirrors *sharply* is a different picture from one carrying a broad smear of sky. |
    | **(b)** | a NEW `_SkyScatterFloor` at **0.077** | 0.331 | 0.208 | **0.088** | 0.098 | Nothing at a calm, by construction — it rises exactly as the mirror falls. Broadband, unsmeared sky: the rough sea gets lighter and *flatter*, not more mirror-like. Costs one new uniform, and this PR does **not** ship it. |
    | **(c1)** | the shared body × **9.5** (`_PaletteDeep` / `_DeepBlueStrength`) | **0.755** | 0.326 | **0.088** | 0.199 | The only option that **cannot** be made free to your glass calm — it more than doubles it (0.331 → 0.755). Against that, the only one needing no new uniform and no widened range: two knobs you already have. |
    | **(c2)** | `Water_StormGrey`'s own master **+0.218** | 0.331 | 0.187 | **0.088** | 0.222 | Nothing at a calm (the storm mood's weight there is 0.000), but it lands hardest of all on the gale: 0.021 → **0.222**, 2.6× whatever it gives the blow. This is the one most likely to **flatten the storm's mood**. Cheapest to ship. |

    A quarter of the way back (blow → 0.044) and all the way back to light airs (blow → 0.176) are
    priced the same way in the PR body and in row 25. ⚠️ **(a) cannot reach all the way on its own**:
    at the widest setting tested it gets a blow to 0.163, not 0.176.

    ⚠️ **A second thing to rule on while you are here.** `_SwellReadStrength` — the swell read you
    lowered the onset for on 2026-09-09 — can only ever make the water **darker**. Zeroing it at a gale
    takes the sea *brighter*, 0.06–0.07 → **0.098**. Whichever option above you pick, a shading term
    with no lit side cannot draw you a face on a dark sea; it can only cut one out of it.

    **Nothing has been chosen.** Say **(a)**, **(b)**, **(c1)**, **(c2)**, or *"none of these — shoot
    me the plate first"*, and the next water PR builds exactly that. If it is (b), it ships gated at 0
    the way the surf dials and `_CapAgeStrength` did, so you turn it up yourself.

---

## 4. Limits of the instrument, stated

- Flat pass only (no vertex lift, no hull, no wake, no spray, no interior mask, no foam buffer pass) — §1.
- One instant: nothing that moves can be judged from it; `_Time` differs between any two plates.
- Moonless: `MoonCycle` needs a clock. A lit-moon row is one publish away (`PublishTheNewMoon` is the hook).
- The weather pairing is the sim's. A plate at #680's 8 m/s / 0.55 is not in the matrix.
- The aims are derived from the shipped terrain and the reference sea (blow, spring low), so they move if the terrain or the wave settings move — the manifest records where each sweep looked.

## 5. Re-running it

```
"C:/Program Files/Unity/Hub/Editor/6000.5.0f1/Editor/Unity.exe" -runTests -batchmode -projectPath <worktree> -testPlatform EditMode -testFilter HiddenHarbours.Tests.EditMode.WaterFidelityPlateSweepTests -testResults <abs>/plates.xml -logFile <abs>/plates.log
```

No `-nographics` (the sweep self-skips on the Null device and reports NOT VERIFIED). ~50 s on the RTX 4060
once the shader cache is warm; the four sheets, 144 plates, the manifests and the diagnostic land under
`<worktree>/artifacts/water-plates/`. Keep the previous run's sheets beside the new ones: the pair is the
evidence.
