using System.Runtime.CompilerServices;

// ArrivalDeck — the IStandableSurface the opening registers under the passenger — is internal, and
// it is the class that decides whether a world point is HER PLANKING or open water. That decision
// used to be a world-axis square of side = her length, 6.95x her drawn deck, and 86.7-88.8% of what
// it called "deck" was sea (owner playtest 2026-09-09). A guard that cannot construct the shipped
// registrant can only re-measure the maths under it, which is the shape of guard that let the square
// stand. Tests only; nothing else should bind these internals.
[assembly: InternalsVisibleTo("HiddenHarbours.Tests.EditMode")]
